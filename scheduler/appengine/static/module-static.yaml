version: 1
module: static
runtime: go
api_version: go1

handlers:
- url: /static
  static_dir: .
  secure: always

- url: /static/bootstrap
  static_dir: bootstrap
  secure: always

- url: /static/jquery
  static_dir: jquery
  secure: always

includes:
  - common

This directory contains configs used when running luci-cron on the local dev
server.

In production configs like these are fetched from the luci-config service
(configured via /admin/settings/gaeconfig page).

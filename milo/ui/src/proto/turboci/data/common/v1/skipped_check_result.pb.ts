// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/data/common/v1/skipped_check_result.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.data.common.v1";

/**
 * A common Check result for skipped checks.
 *
 * This Result should be included on a check while marking it FINAL, and should
 * be taken as an indicator that this check was intentionally skipped by the
 * workflow, with the given reasons.
 *
 * This common Check result is mainly for Chronical UI. If you have some other
 * machine readabale structured reasons for skipping the check, Please use
 * another CheckResult to capture that.
 */
export interface SkippedCheckResult {
  /** Human readable string message on why the check was skipped. */
  readonly message?: string | undefined;
}

function createBaseSkippedCheckResult(): SkippedCheckResult {
  return { message: undefined };
}

export const SkippedCheckResult: MessageFns<SkippedCheckResult> = {
  encode(message: SkippedCheckResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== undefined) {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkippedCheckResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkippedCheckResult() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkippedCheckResult {
    return { message: isSet(object.message) ? globalThis.String(object.message) : undefined };
  },

  toJSON(message: SkippedCheckResult): unknown {
    const obj: any = {};
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<SkippedCheckResult>): SkippedCheckResult {
    return SkippedCheckResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkippedCheckResult>): SkippedCheckResult {
    const message = createBaseSkippedCheckResult() as any;
    message.message = object.message ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

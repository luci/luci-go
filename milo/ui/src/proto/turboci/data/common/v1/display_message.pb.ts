// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.32.1
// source: turboci/data/common/v1/display_message.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.data.common.v1";

/** A message string and a format-type enum to instruct clients how to render the text. */
export interface DisplayMessage {
  /** The message string. */
  readonly message?:
    | string
    | undefined;
  /** How the message string should be formatted. */
  readonly messageFormat?: DisplayMessage_MessageFormat | undefined;
}

/** Different types of message formatting. */
export enum DisplayMessage_MessageFormat {
  /** MESSAGE_FORMAT_UNKNOWN - Default message format. */
  MESSAGE_FORMAT_UNKNOWN = 0,
  /** MESSAGE_FORMAT_PLAIN_TEXT - Plain text - no special formatting. */
  MESSAGE_FORMAT_PLAIN_TEXT = 1,
  /** MESSAGE_FORMAT_HTML - HTML markup. */
  MESSAGE_FORMAT_HTML = 2,
  /** MESSAGE_FORMAT_MARKDOWN - Markdown formatting. */
  MESSAGE_FORMAT_MARKDOWN = 3,
  /** MESSAGE_FORMAT_ANSI - Includes ANSI escape codes for controlling things like text color. */
  MESSAGE_FORMAT_ANSI = 4,
}

export function displayMessage_MessageFormatFromJSON(object: any): DisplayMessage_MessageFormat {
  switch (object) {
    case 0:
    case "MESSAGE_FORMAT_UNKNOWN":
      return DisplayMessage_MessageFormat.MESSAGE_FORMAT_UNKNOWN;
    case 1:
    case "MESSAGE_FORMAT_PLAIN_TEXT":
      return DisplayMessage_MessageFormat.MESSAGE_FORMAT_PLAIN_TEXT;
    case 2:
    case "MESSAGE_FORMAT_HTML":
      return DisplayMessage_MessageFormat.MESSAGE_FORMAT_HTML;
    case 3:
    case "MESSAGE_FORMAT_MARKDOWN":
      return DisplayMessage_MessageFormat.MESSAGE_FORMAT_MARKDOWN;
    case 4:
    case "MESSAGE_FORMAT_ANSI":
      return DisplayMessage_MessageFormat.MESSAGE_FORMAT_ANSI;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum DisplayMessage_MessageFormat");
  }
}

export function displayMessage_MessageFormatToJSON(object: DisplayMessage_MessageFormat): string {
  switch (object) {
    case DisplayMessage_MessageFormat.MESSAGE_FORMAT_UNKNOWN:
      return "MESSAGE_FORMAT_UNKNOWN";
    case DisplayMessage_MessageFormat.MESSAGE_FORMAT_PLAIN_TEXT:
      return "MESSAGE_FORMAT_PLAIN_TEXT";
    case DisplayMessage_MessageFormat.MESSAGE_FORMAT_HTML:
      return "MESSAGE_FORMAT_HTML";
    case DisplayMessage_MessageFormat.MESSAGE_FORMAT_MARKDOWN:
      return "MESSAGE_FORMAT_MARKDOWN";
    case DisplayMessage_MessageFormat.MESSAGE_FORMAT_ANSI:
      return "MESSAGE_FORMAT_ANSI";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum DisplayMessage_MessageFormat");
  }
}

function createBaseDisplayMessage(): DisplayMessage {
  return { message: undefined, messageFormat: undefined };
}

export const DisplayMessage: MessageFns<DisplayMessage> = {
  encode(message: DisplayMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== undefined) {
      writer.uint32(10).string(message.message);
    }
    if (message.messageFormat !== undefined) {
      writer.uint32(16).int32(message.messageFormat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DisplayMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisplayMessage() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.messageFormat = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DisplayMessage {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
      messageFormat: isSet(object.messageFormat)
        ? displayMessage_MessageFormatFromJSON(object.messageFormat)
        : undefined,
    };
  },

  toJSON(message: DisplayMessage): unknown {
    const obj: any = {};
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    if (message.messageFormat !== undefined) {
      obj.messageFormat = displayMessage_MessageFormatToJSON(message.messageFormat);
    }
    return obj;
  },

  create(base?: DeepPartial<DisplayMessage>): DisplayMessage {
    return DisplayMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DisplayMessage>): DisplayMessage {
    const message = createBaseDisplayMessage() as any;
    message.message = object.message ?? undefined;
    message.messageFormat = object.messageFormat ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

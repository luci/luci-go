// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.32.1
// source: turboci/data/chrome/depot_tools/v1/bot_update_results.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.data.chrome.depot_tools.v1";

/**
 * BotUpdateResults provides details about all of the repositories that were
 * checked out. bot_update checks out one or more repositories to a common
 * directory, each of which can have have additional repos checked out either
 * via DEPS files or submodules and so on recursively. Any repo in the tree can
 * have its revision overridden.
 */
export interface BotUpdateResults {
  /**
   * The revision information for all git repos that bot_update checked out. The
   * key is the path from the common directory where the checkout was performed,
   * with the value providing information about the repository and revision that
   * was checked out.
   */
  readonly manifest: { [key: string]: BotUpdateResults_GitCommit };
}

/**
 * Information about the commit checked out for a repo (ignoring any patch
 * applied).
 */
export interface BotUpdateResults_GitCommit {
  /**
   * GoB host without the ".googlesource.com" hostname suffix as the
   * suffix may be different depending on how it's accessed (eg.
   * ".git.corp.google.com") while still referring to the same repo.
   */
  readonly host?:
    | string
    | undefined;
  /** Repository name on the host, e.g. "chromium/src". */
  readonly project?:
    | string
    | undefined;
  /** Commit ID (eg. HEX SHA1). */
  readonly id?: string | undefined;
}

export interface BotUpdateResults_ManifestEntry {
  readonly key: string;
  readonly value: BotUpdateResults_GitCommit | undefined;
}

function createBaseBotUpdateResults(): BotUpdateResults {
  return { manifest: {} };
}

export const BotUpdateResults: MessageFns<BotUpdateResults> = {
  encode(message: BotUpdateResults, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    Object.entries(message.manifest).forEach(([key, value]) => {
      BotUpdateResults_ManifestEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BotUpdateResults {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBotUpdateResults() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = BotUpdateResults_ManifestEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.manifest[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BotUpdateResults {
    return {
      manifest: isObject(object.manifest)
        ? Object.entries(object.manifest).reduce<{ [key: string]: BotUpdateResults_GitCommit }>((acc, [key, value]) => {
          acc[key] = BotUpdateResults_GitCommit.fromJSON(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: BotUpdateResults): unknown {
    const obj: any = {};
    if (message.manifest) {
      const entries = Object.entries(message.manifest);
      if (entries.length > 0) {
        obj.manifest = {};
        entries.forEach(([k, v]) => {
          obj.manifest[k] = BotUpdateResults_GitCommit.toJSON(v);
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<BotUpdateResults>): BotUpdateResults {
    return BotUpdateResults.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BotUpdateResults>): BotUpdateResults {
    const message = createBaseBotUpdateResults() as any;
    message.manifest = Object.entries(object.manifest ?? {}).reduce<{ [key: string]: BotUpdateResults_GitCommit }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = BotUpdateResults_GitCommit.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseBotUpdateResults_GitCommit(): BotUpdateResults_GitCommit {
  return { host: undefined, project: undefined, id: undefined };
}

export const BotUpdateResults_GitCommit: MessageFns<BotUpdateResults_GitCommit> = {
  encode(message: BotUpdateResults_GitCommit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== undefined) {
      writer.uint32(10).string(message.host);
    }
    if (message.project !== undefined) {
      writer.uint32(18).string(message.project);
    }
    if (message.id !== undefined) {
      writer.uint32(26).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BotUpdateResults_GitCommit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBotUpdateResults_GitCommit() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BotUpdateResults_GitCommit {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : undefined,
      project: isSet(object.project) ? globalThis.String(object.project) : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
    };
  },

  toJSON(message: BotUpdateResults_GitCommit): unknown {
    const obj: any = {};
    if (message.host !== undefined) {
      obj.host = message.host;
    }
    if (message.project !== undefined) {
      obj.project = message.project;
    }
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<BotUpdateResults_GitCommit>): BotUpdateResults_GitCommit {
    return BotUpdateResults_GitCommit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BotUpdateResults_GitCommit>): BotUpdateResults_GitCommit {
    const message = createBaseBotUpdateResults_GitCommit() as any;
    message.host = object.host ?? undefined;
    message.project = object.project ?? undefined;
    message.id = object.id ?? undefined;
    return message;
  },
};

function createBaseBotUpdateResults_ManifestEntry(): BotUpdateResults_ManifestEntry {
  return { key: "", value: undefined };
}

export const BotUpdateResults_ManifestEntry: MessageFns<BotUpdateResults_ManifestEntry> = {
  encode(message: BotUpdateResults_ManifestEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      BotUpdateResults_GitCommit.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BotUpdateResults_ManifestEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBotUpdateResults_ManifestEntry() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = BotUpdateResults_GitCommit.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BotUpdateResults_ManifestEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? BotUpdateResults_GitCommit.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: BotUpdateResults_ManifestEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = BotUpdateResults_GitCommit.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<BotUpdateResults_ManifestEntry>): BotUpdateResults_ManifestEntry {
    return BotUpdateResults_ManifestEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BotUpdateResults_ManifestEntry>): BotUpdateResults_ManifestEntry {
    const message = createBaseBotUpdateResults_ManifestEntry() as any;
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? BotUpdateResults_GitCommit.fromPartial(object.value)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/data/chrome/build/v1/analyze_options.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.data.chrome.build.v1";

/**
 * AnalyzeOptions provides inputs to analyze. Analyze is used to reduce the set
 * of compile targets that are built to only those that have a build graph
 * dependency on a file changed by the CL under test.
 */
export interface AnalyzeOptions {
  /** The compile targets that are explicitly requested for compilation. */
  readonly compileTargets: readonly string[];
  /** The compile targets corresponding to the tests to potentially execute. */
  readonly testTargets: readonly string[];
  /**
   * A path relative to the top-level repo to a file with configuration for
   * affected files that cause analyze to be skipped. The path should be
   * relative to the root of the top-level repo.
   *
   * The format of the file is a json object with attributes that are analyze
   * configs. An analyze config with the name "base" must be present and will
   * always be used. Additional analyze configs may be present and will be used
   * if the name is present in analyze_names.
   *
   * Each analyze config is a json object that may contain "exclusions" and/or
   * "ignores" attributes. The value of either attribute is a list of regexes
   * that will be matched against the affected file paths . For each file path,
   * if the entire path matches an exclusion regex and doesn't match any ignore
   * regexes then analyze will be skipped, causing all of the input compile
   * targets to be built.
   */
  readonly analyzeConfigPath?:
    | string
    | undefined;
  /** Names of additional analyze configs to use. */
  readonly analyzeConfigNames: readonly string[];
  /**
   * Additional exclusions provided by the workflow. The keys are the file-path
   * relative to the top-level repo with the values being a description of the
   * reason for the exclusion.
   */
  readonly additionalExclusions: { [key: string]: string };
}

export interface AnalyzeOptions_AdditionalExclusionsEntry {
  readonly key: string;
  readonly value: string;
}

function createBaseAnalyzeOptions(): AnalyzeOptions {
  return {
    compileTargets: [],
    testTargets: [],
    analyzeConfigPath: undefined,
    analyzeConfigNames: [],
    additionalExclusions: {},
  };
}

export const AnalyzeOptions: MessageFns<AnalyzeOptions> = {
  encode(message: AnalyzeOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.compileTargets) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.testTargets) {
      writer.uint32(18).string(v!);
    }
    if (message.analyzeConfigPath !== undefined) {
      writer.uint32(26).string(message.analyzeConfigPath);
    }
    for (const v of message.analyzeConfigNames) {
      writer.uint32(34).string(v!);
    }
    Object.entries(message.additionalExclusions).forEach(([key, value]) => {
      AnalyzeOptions_AdditionalExclusionsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyzeOptions() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.compileTargets.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.testTargets.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.analyzeConfigPath = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.analyzeConfigNames.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = AnalyzeOptions_AdditionalExclusionsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.additionalExclusions[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyzeOptions {
    return {
      compileTargets: globalThis.Array.isArray(object?.compileTargets)
        ? object.compileTargets.map((e: any) => globalThis.String(e))
        : [],
      testTargets: globalThis.Array.isArray(object?.testTargets)
        ? object.testTargets.map((e: any) => globalThis.String(e))
        : [],
      analyzeConfigPath: isSet(object.analyzeConfigPath) ? globalThis.String(object.analyzeConfigPath) : undefined,
      analyzeConfigNames: globalThis.Array.isArray(object?.analyzeConfigNames)
        ? object.analyzeConfigNames.map((e: any) => globalThis.String(e))
        : [],
      additionalExclusions: isObject(object.additionalExclusions)
        ? Object.entries(object.additionalExclusions).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: AnalyzeOptions): unknown {
    const obj: any = {};
    if (message.compileTargets?.length) {
      obj.compileTargets = message.compileTargets;
    }
    if (message.testTargets?.length) {
      obj.testTargets = message.testTargets;
    }
    if (message.analyzeConfigPath !== undefined) {
      obj.analyzeConfigPath = message.analyzeConfigPath;
    }
    if (message.analyzeConfigNames?.length) {
      obj.analyzeConfigNames = message.analyzeConfigNames;
    }
    if (message.additionalExclusions) {
      const entries = Object.entries(message.additionalExclusions);
      if (entries.length > 0) {
        obj.additionalExclusions = {};
        entries.forEach(([k, v]) => {
          obj.additionalExclusions[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<AnalyzeOptions>): AnalyzeOptions {
    return AnalyzeOptions.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalyzeOptions>): AnalyzeOptions {
    const message = createBaseAnalyzeOptions() as any;
    message.compileTargets = object.compileTargets?.map((e) => e) || [];
    message.testTargets = object.testTargets?.map((e) => e) || [];
    message.analyzeConfigPath = object.analyzeConfigPath ?? undefined;
    message.analyzeConfigNames = object.analyzeConfigNames?.map((e) => e) || [];
    message.additionalExclusions = Object.entries(object.additionalExclusions ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseAnalyzeOptions_AdditionalExclusionsEntry(): AnalyzeOptions_AdditionalExclusionsEntry {
  return { key: "", value: "" };
}

export const AnalyzeOptions_AdditionalExclusionsEntry: MessageFns<AnalyzeOptions_AdditionalExclusionsEntry> = {
  encode(message: AnalyzeOptions_AdditionalExclusionsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeOptions_AdditionalExclusionsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyzeOptions_AdditionalExclusionsEntry() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyzeOptions_AdditionalExclusionsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AnalyzeOptions_AdditionalExclusionsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<AnalyzeOptions_AdditionalExclusionsEntry>): AnalyzeOptions_AdditionalExclusionsEntry {
    return AnalyzeOptions_AdditionalExclusionsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalyzeOptions_AdditionalExclusionsEntry>): AnalyzeOptions_AdditionalExclusionsEntry {
    const message = createBaseAnalyzeOptions_AdditionalExclusionsEntry() as any;
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

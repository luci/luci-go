// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/allocate_worknode_ids_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Stage } from "../../ids/v1/identifier.pb";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/** Response message for TurboCIOrchestrator.AllocateWorkNodeIDs. */
export interface AllocateWorkNodeIDsResponse {
  /**
   * Generated WorkNode stage IDs.
   *
   * They have `work_plan` populated and `is_worknode` bit set to true.
   */
  readonly ids: readonly Stage[];
}

function createBaseAllocateWorkNodeIDsResponse(): AllocateWorkNodeIDsResponse {
  return { ids: [] };
}

export const AllocateWorkNodeIDsResponse: MessageFns<AllocateWorkNodeIDsResponse> = {
  encode(message: AllocateWorkNodeIDsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ids) {
      Stage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllocateWorkNodeIDsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllocateWorkNodeIDsResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ids.push(Stage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllocateWorkNodeIDsResponse {
    return { ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => Stage.fromJSON(e)) : [] };
  },

  toJSON(message: AllocateWorkNodeIDsResponse): unknown {
    const obj: any = {};
    if (message.ids?.length) {
      obj.ids = message.ids.map((e) => Stage.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AllocateWorkNodeIDsResponse>): AllocateWorkNodeIDsResponse {
    return AllocateWorkNodeIDsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AllocateWorkNodeIDsResponse>): AllocateWorkNodeIDsResponse {
    const message = createBaseAllocateWorkNodeIDsResponse() as any;
    message.ids = object.ids?.map((e) => Stage.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

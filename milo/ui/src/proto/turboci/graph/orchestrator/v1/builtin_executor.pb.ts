// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/builtin_executor.proto

/* eslint-disable */

export const protobufPackage = "turboci.graph.orchestrator.v1";

/** BuiltinExecutor enumerates stage kinds that Orchestrator implements natively. */
export enum BuiltinExecutor {
  /** BUILTIN_EXECUTOR_UNKNOWN - UNKNOWN is the same as "the stage is implemented by an external executor". */
  BUILTIN_EXECUTOR_UNKNOWN = 0,
  /**
   * BUILTIN_EXECUTOR_NOOP - This executor just immediately finishes the stage (with a single COMPLETE
   * attempt) as soon as the stage is unblocked.
   *
   * By being a builtin executor, it can do so in the same transaction that
   * unblocks the stage, skipping a lot of overhead associated with actually
   * launching a real attempt.
   *
   * Noop stage are useful for grouping dependencies. A stage with a particular
   * args type URL can be defined as a noop by setting `noop: true` in the stage
   * registry config entry for this type URL.
   */
  BUILTIN_EXECUTOR_NOOP = 1,
}

export function builtinExecutorFromJSON(object: any): BuiltinExecutor {
  switch (object) {
    case 0:
    case "BUILTIN_EXECUTOR_UNKNOWN":
      return BuiltinExecutor.BUILTIN_EXECUTOR_UNKNOWN;
    case 1:
    case "BUILTIN_EXECUTOR_NOOP":
      return BuiltinExecutor.BUILTIN_EXECUTOR_NOOP;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum BuiltinExecutor");
  }
}

export function builtinExecutorToJSON(object: BuiltinExecutor): string {
  switch (object) {
    case BuiltinExecutor.BUILTIN_EXECUTOR_UNKNOWN:
      return "BUILTIN_EXECUTOR_UNKNOWN";
    case BuiltinExecutor.BUILTIN_EXECUTOR_NOOP:
      return "BUILTIN_EXECUTOR_NOOP";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum BuiltinExecutor");
  }
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/type_set.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/** TypeSet represents a set of possible type_urls of Values. */
export interface TypeSet {
  /**
   * A list of type URLs or type URL wildcards (if ends with `*`).
   *
   * Examples are:
   *   - type.googleapis.com/turboci.data.stage.v1.CommonStageAttemptDetails
   *   - type.googleapis.com/turboci.data.stage.v1.*
   *
   * Note that using wildcard type sets may be restricted by the particular
   * API that handles the TypeSet.
   */
  readonly typeUrls: readonly string[];
}

function createBaseTypeSet(): TypeSet {
  return { typeUrls: [] };
}

export const TypeSet: MessageFns<TypeSet> = {
  encode(message: TypeSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.typeUrls) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TypeSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTypeSet() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.typeUrls.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TypeSet {
    return {
      typeUrls: globalThis.Array.isArray(object?.typeUrls) ? object.typeUrls.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: TypeSet): unknown {
    const obj: any = {};
    if (message.typeUrls?.length) {
      obj.typeUrls = message.typeUrls;
    }
    return obj;
  },

  create(base?: DeepPartial<TypeSet>): TypeSet {
    return TypeSet.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TypeSet>): TypeSet {
    const message = createBaseTypeSet() as any;
    message.typeUrls = object.typeUrls?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

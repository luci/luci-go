// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/read_workplan_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { WorkPlan } from "../../ids/v1/identifier.pb";
import { IdentifierKind, identifierKindFromJSON, identifierKindToJSON } from "../../ids/v1/identifier_kind.pb";
import { Revision } from "./revision.pb";
import { ValueFilter } from "./value_filter.pb";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/** Request message for TurboCIGraphService.ReadWorkPlan. */
export interface ReadWorkPlanRequest {
  /**
   * The token which indicates that the request is coming from within a specific
   * WorkPlan.
   *
   * The token may be either a stage attempt token or a workplan creator token.
   * A suitable token is provided to the Executor of a Stage or to the creator
   * of an empty Workplan.
   *
   * This is in addition to regular RPC authorization.
   *
   * If the token is populated:
   * * If its workplan ID matches the workplan ID in the request, this RPC will
   *   check that the caller has the relevant 'read' permissions.
   * * If it doesn't match, this RPC will reject the request.
   * If the token is not populated, the relevant 'readExternal' permissions will
   * be checked.
   */
  readonly token?:
    | string
    | undefined;
  /** The ID of the workplan from which to select nodes to return. */
  readonly workplanId?:
    | WorkPlan
    | undefined;
  /**
   * The set of node types for which we should return nodes. If none are
   * provided, the request will be rejected with INVALID_ARGUMENT.
   *
   * These identifier types are not supported, should not be requested and will
   * be ignored:
   * - WorkPlans. The associated WorkPlan will always be returned.
   * - CheckResults. They will always be returned as part of their parent Check.
   *
   * Because StageAttempts are returned nested in their parent Stage, any
   * StageAttempts returned will have their parent Stage included even if that
   * Stage would otherwise have been excluded from the response (including if
   * IDENTIFIER_KIND_STAGE is not specified).
   */
  readonly includedNodeTypes: readonly IdentifierKind[];
  /**
   * Returns only nodes whose Revision is > this value. If omitted or set to 0,
   * returns all nodes matching the request irrespective of their revision.
   *
   * This allows a caller that has previously retrieved a workplan's nodes to
   * retrieve only nodes that have changed since the revision at which that
   * previous retrieval was performed.
   */
  readonly sinceVersion?:
    | Revision
    | undefined;
  /**
   * Describes how ValueData messages within the node types matching the query
   * criteria should be filtered in/out of the response. All other fields of
   * ValueRef messages will always be included.
   *
   * ValueData messages will be included only if:
   * * Their containing node is included via `included_node_types`
   * * AND their `type_url` matches the expression in `ValueFilter.wanted`
   * * AND their containing node type's ValueMask in ValueFilter includes DATA
   */
  readonly valueFilter?:
    | ValueFilter
    | undefined;
  /**
   * Pagination token, so this query can resume consuming nodes with the next
   * node in the stream. If omitted, returns nodes beginning with the first node
   * in the stream.
   *
   * If provided, the other fields of the ReadWorkPlanRequest need to match
   * their values from the first page of this request. Attempting to use a
   * pagination token while changing any other attributes of the request may
   * result in unexpected or inconsistent results.
   */
  readonly paginationToken?: string | undefined;
}

function createBaseReadWorkPlanRequest(): ReadWorkPlanRequest {
  return {
    token: undefined,
    workplanId: undefined,
    includedNodeTypes: [],
    sinceVersion: undefined,
    valueFilter: undefined,
    paginationToken: undefined,
  };
}

export const ReadWorkPlanRequest: MessageFns<ReadWorkPlanRequest> = {
  encode(message: ReadWorkPlanRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== undefined) {
      writer.uint32(10).string(message.token);
    }
    if (message.workplanId !== undefined) {
      WorkPlan.encode(message.workplanId, writer.uint32(18).fork()).join();
    }
    for (const v of message.includedNodeTypes) {
      writer.uint32(24).int32(v!);
    }
    if (message.sinceVersion !== undefined) {
      Revision.encode(message.sinceVersion, writer.uint32(34).fork()).join();
    }
    if (message.valueFilter !== undefined) {
      ValueFilter.encode(message.valueFilter, writer.uint32(42).fork()).join();
    }
    if (message.paginationToken !== undefined) {
      writer.uint32(50).string(message.paginationToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadWorkPlanRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadWorkPlanRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workplanId = WorkPlan.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.includedNodeTypes.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.includedNodeTypes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sinceVersion = Revision.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.valueFilter = ValueFilter.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.paginationToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadWorkPlanRequest {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : undefined,
      workplanId: isSet(object.workplanId) ? WorkPlan.fromJSON(object.workplanId) : undefined,
      includedNodeTypes: globalThis.Array.isArray(object?.includedNodeTypes)
        ? object.includedNodeTypes.map((e: any) => identifierKindFromJSON(e))
        : [],
      sinceVersion: isSet(object.sinceVersion) ? Revision.fromJSON(object.sinceVersion) : undefined,
      valueFilter: isSet(object.valueFilter) ? ValueFilter.fromJSON(object.valueFilter) : undefined,
      paginationToken: isSet(object.paginationToken) ? globalThis.String(object.paginationToken) : undefined,
    };
  },

  toJSON(message: ReadWorkPlanRequest): unknown {
    const obj: any = {};
    if (message.token !== undefined) {
      obj.token = message.token;
    }
    if (message.workplanId !== undefined) {
      obj.workplanId = WorkPlan.toJSON(message.workplanId);
    }
    if (message.includedNodeTypes?.length) {
      obj.includedNodeTypes = message.includedNodeTypes.map((e) => identifierKindToJSON(e));
    }
    if (message.sinceVersion !== undefined) {
      obj.sinceVersion = Revision.toJSON(message.sinceVersion);
    }
    if (message.valueFilter !== undefined) {
      obj.valueFilter = ValueFilter.toJSON(message.valueFilter);
    }
    if (message.paginationToken !== undefined) {
      obj.paginationToken = message.paginationToken;
    }
    return obj;
  },

  create(base?: DeepPartial<ReadWorkPlanRequest>): ReadWorkPlanRequest {
    return ReadWorkPlanRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReadWorkPlanRequest>): ReadWorkPlanRequest {
    const message = createBaseReadWorkPlanRequest() as any;
    message.token = object.token ?? undefined;
    message.workplanId = (object.workplanId !== undefined && object.workplanId !== null)
      ? WorkPlan.fromPartial(object.workplanId)
      : undefined;
    message.includedNodeTypes = object.includedNodeTypes?.map((e) => e) || [];
    message.sinceVersion = (object.sinceVersion !== undefined && object.sinceVersion !== null)
      ? Revision.fromPartial(object.sinceVersion)
      : undefined;
    message.valueFilter = (object.valueFilter !== undefined && object.valueFilter !== null)
      ? ValueFilter.fromPartial(object.valueFilter)
      : undefined;
    message.paginationToken = object.paginationToken ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/value_digest.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/**
 * ValueHashAlgo identifies the type of hash used for a particular ValueRef.
 *
 * The server may only implement a subset of these.
 */
export enum ValueHashAlgo {
  /** VALUE_HASH_ALGO_UNKNOWN - Unknown, invalid, hash algorithm. */
  VALUE_HASH_ALGO_UNKNOWN = 0,
  /**
   * VALUE_HASH_ALGO_SHA256 - SHA256(Any())
   *
   * The Any is serialized the same way that the Bazel Remote Build API defines
   * hashing protos, namely:
   *
   * * Fields are serialized in tag order.
   * * There are no unknown fields.
   * * There are no duplicate fields.
   * * Fields are serialized according to the default semantics for their type.
   *
   * Specifically, this means that this is computed as:
   *
   *   SHA256(protoTag(1, bytes) || type_url || protoTag(2, bytes) || value)
   *
   * Which is the *typical* encoding of a `google.protobuf.Any` using standard
   * serializers.
   *
   * len(hash) = 32
   */
  VALUE_HASH_ALGO_SHA256 = 1,
}

export function valueHashAlgoFromJSON(object: any): ValueHashAlgo {
  switch (object) {
    case 0:
    case "VALUE_HASH_ALGO_UNKNOWN":
      return ValueHashAlgo.VALUE_HASH_ALGO_UNKNOWN;
    case 1:
    case "VALUE_HASH_ALGO_SHA256":
      return ValueHashAlgo.VALUE_HASH_ALGO_SHA256;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ValueHashAlgo");
  }
}

export function valueHashAlgoToJSON(object: ValueHashAlgo): string {
  switch (object) {
    case ValueHashAlgo.VALUE_HASH_ALGO_UNKNOWN:
      return "VALUE_HASH_ALGO_UNKNOWN";
    case ValueHashAlgo.VALUE_HASH_ALGO_SHA256:
      return "VALUE_HASH_ALGO_SHA256";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ValueHashAlgo");
  }
}

/**
 * ValueDigest is the de-serialized form of a ValueRef.digest.
 *
 * The TurboCI API always uses these in their serialized (URL safe string)
 * form, not as protos - however this message is included because it is
 * occasionally useful to extract the raw hash and/or size of the original
 * hashed data.
 *
 * Serialized as:
 *
 *   BASE64_URLSAFE(hash || uvarint(size_bytes) || byte(algo))
 *
 * And de-serialized as:
 *
 *   rawDecoded = BASE64_URLSAFE_dec(encoded)
 *   algo = ALGOS[rawDecoded[-1]]
 *   rawHash = rawDecoded[algo.len]
 *   size = uvarintDecode(rawDecoded[algo.len:-1])
 *
 * This puts `hash` at the front, rather than `algo`, in order to help uses of
 * the ValueDigest as a primary key to avoid hotspotting. For the same reason,
 * the algo is always put at the end because it will have the lowest entropy
 * (almost certainly will always be the value '1').
 */
export interface ValueDigest {
  /** Required. The raw bytes of the hash. */
  readonly hash?:
    | Uint8Array
    | undefined;
  /** Required. The size of the hashed data in bytes. */
  readonly sizeBytes?:
    | string
    | undefined;
  /** Required. The algorithm used to compute `hash`. */
  readonly algo?: ValueHashAlgo | undefined;
}

function createBaseValueDigest(): ValueDigest {
  return { hash: undefined, sizeBytes: undefined, algo: undefined };
}

export const ValueDigest: MessageFns<ValueDigest> = {
  encode(message: ValueDigest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== undefined) {
      writer.uint32(10).bytes(message.hash);
    }
    if (message.sizeBytes !== undefined) {
      writer.uint32(16).uint64(message.sizeBytes);
    }
    if (message.algo !== undefined) {
      writer.uint32(24).int32(message.algo);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValueDigest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValueDigest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sizeBytes = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.algo = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValueDigest {
    return {
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : undefined,
      sizeBytes: isSet(object.sizeBytes) ? globalThis.String(object.sizeBytes) : undefined,
      algo: isSet(object.algo) ? valueHashAlgoFromJSON(object.algo) : undefined,
    };
  },

  toJSON(message: ValueDigest): unknown {
    const obj: any = {};
    if (message.hash !== undefined) {
      obj.hash = base64FromBytes(message.hash);
    }
    if (message.sizeBytes !== undefined) {
      obj.sizeBytes = message.sizeBytes;
    }
    if (message.algo !== undefined) {
      obj.algo = valueHashAlgoToJSON(message.algo);
    }
    return obj;
  },

  create(base?: DeepPartial<ValueDigest>): ValueDigest {
    return ValueDigest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ValueDigest>): ValueDigest {
    const message = createBaseValueDigest() as any;
    message.hash = object.hash ?? undefined;
    message.sizeBytes = object.sizeBytes ?? undefined;
    message.algo = object.algo ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/allocate_worknode_ids_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { WorkPlan } from "../../ids/v1/identifier.pb";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/** Request message for TurboCIOrchestrator.AllocateWorkNodeIDs. */
export interface AllocateWorkNodeIDsRequest {
  /**
   * The token which constrains this request to a specific Workplan.
   *
   * A suitable token is provided to the Executor of a Stage or to the creator
   * of an empty Workplan.
   *
   * This is in addition to regular RPC authorization.
   */
  readonly token?:
    | string
    | undefined;
  /**
   * The ID of the work plan to generate node IDs in.
   *
   * Can be omitted if `token` is set. If both `token` and `workplan_id` are
   * set, plan IDs must match.
   */
  readonly workplanId?:
    | WorkPlan
    | undefined;
  /**
   * How many work node IDs to generate.
   *
   * Must be larger than 0 and no more than 1000.
   */
  readonly count?: number | undefined;
}

function createBaseAllocateWorkNodeIDsRequest(): AllocateWorkNodeIDsRequest {
  return { token: undefined, workplanId: undefined, count: undefined };
}

export const AllocateWorkNodeIDsRequest: MessageFns<AllocateWorkNodeIDsRequest> = {
  encode(message: AllocateWorkNodeIDsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== undefined) {
      writer.uint32(10).string(message.token);
    }
    if (message.workplanId !== undefined) {
      WorkPlan.encode(message.workplanId, writer.uint32(18).fork()).join();
    }
    if (message.count !== undefined) {
      writer.uint32(24).int32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllocateWorkNodeIDsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllocateWorkNodeIDsRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workplanId = WorkPlan.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllocateWorkNodeIDsRequest {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : undefined,
      workplanId: isSet(object.workplanId) ? WorkPlan.fromJSON(object.workplanId) : undefined,
      count: isSet(object.count) ? globalThis.Number(object.count) : undefined,
    };
  },

  toJSON(message: AllocateWorkNodeIDsRequest): unknown {
    const obj: any = {};
    if (message.token !== undefined) {
      obj.token = message.token;
    }
    if (message.workplanId !== undefined) {
      obj.workplanId = WorkPlan.toJSON(message.workplanId);
    }
    if (message.count !== undefined) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<AllocateWorkNodeIDsRequest>): AllocateWorkNodeIDsRequest {
    return AllocateWorkNodeIDsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AllocateWorkNodeIDsRequest>): AllocateWorkNodeIDsRequest {
    const message = createBaseAllocateWorkNodeIDsRequest() as any;
    message.token = object.token ?? undefined;
    message.workplanId = (object.workplanId !== undefined && object.workplanId !== null)
      ? WorkPlan.fromPartial(object.workplanId)
      : undefined;
    message.count = object.count ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

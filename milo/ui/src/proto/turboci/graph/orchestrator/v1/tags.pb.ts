// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/tags.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/**
 * ReadScope indicates where something (Tag key or value) is readable.
 *
 * The zero value is meaningful (and default); Suffixing it with UNKNOWN does
 * not improve anything.
 *
 * buf:lint:ignore ENUM_ZERO_VALUE_SUFFIX
 */
export enum ReadScope {
  /**
   * READ_SCOPE_VALUE_REF - The default value; Item will be readable on the ValueRef which contains it
   * (e.g. usable for Edge conditions).
   */
  READ_SCOPE_VALUE_REF = 0,
  /**
   * READ_SCOPE_NODE - Readable at the Node level (allowing search for tagged nodes within a
   * WorkPlan via QueryNodes).
   *
   * Also stored in the ValueRef itself (see READ_SCOPE_VALUE_REF).
   *
   * Writing a tag via a Check Option, and then later removing it by
   * overwriting the Option without that tag (e.g. while the Check is still
   * PLANNING) will make it so that searching for this Tag will no longer yield
   * that Check.
   */
  READ_SCOPE_NODE = 1,
  /**
   * READ_SCOPE_WORK_PLAN - Readable at the WorkPlan level (allowing search for tagged WorkPlans via
   * QueryWorkPlans).
   *
   * Also readable at the Node level and the ValueRef itself
   * (READ_SCOPE_NODE and READ_SCOPE_VALUE_REF respectively).
   *
   * These tags will only be searchable at the WorkPlan level once they belong
   * to some *immutable* piece of the WorkPlan (e.g. ValueWrites in a Stage
   * Attempt, Check Options for a Check in the PLANNED state or later, etc.)
   *
   * WorkPlan-scoped tags should be used sparingly, as they are more costly
   * to write performance than Node or ValueRef-scoped tags.
   */
  READ_SCOPE_WORK_PLAN = 2,
}

export function readScopeFromJSON(object: any): ReadScope {
  switch (object) {
    case 0:
    case "READ_SCOPE_VALUE_REF":
      return ReadScope.READ_SCOPE_VALUE_REF;
    case 1:
    case "READ_SCOPE_NODE":
      return ReadScope.READ_SCOPE_NODE;
    case 2:
    case "READ_SCOPE_WORK_PLAN":
      return ReadScope.READ_SCOPE_WORK_PLAN;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ReadScope");
  }
}

export function readScopeToJSON(object: ReadScope): string {
  switch (object) {
    case ReadScope.READ_SCOPE_VALUE_REF:
      return "READ_SCOPE_VALUE_REF";
    case ReadScope.READ_SCOPE_NODE:
      return "READ_SCOPE_NODE";
    case ReadScope.READ_SCOPE_WORK_PLAN:
      return "READ_SCOPE_WORK_PLAN";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum ReadScope");
  }
}

/**
 * Tags allow WorkPlans to export queryable data from a ValueWrite.
 *
 * A given ValueWrite can export zero or more tags.
 *
 * Tags are multi-valued (i.e. it is allowed to have multiple values all with
 * the same key, even on the same ValueWrite).
 *
 * A Tag has a key which should be the protobuf 'full name' of the field from
 * which it originated. This will be some path like
 * `full.proto.package.MessageName.field_name`.
 *
 * In addition to the key, a Tag has one or more values.
 *
 * These Tags and their Values are retained on the ValueRef which results from
 * the ValueWrite, and are visible as long as you have read access to that
 * ValueRef. Edge conditions which point to a node may use these tags and
 * their values as filtering criteria.
 *
 * Tags and their values *may* additionally be indexed to allow using them as
 * filter criteria in QueryWorkPlans or QueryNodes. It's recommended to keep
 * index scopes as narrow as possible.
 *
 * If using one of the TurboCI client libraries, you may annotate your value
 * protobufs with the `turboci.tag` field annotation in order to have the
 * library *automatically* extract tags as a side effect of serializing a proto
 * via ValueWrite.
 */
export interface Tag {
  /**
   * Key should be the protobuf 'full name' of the proto field from which
   * this tag originated.
   */
  readonly key?:
    | string
    | undefined;
  /**
   * Scope indicates the level at which the tag *key* can be read.
   *
   * Defaults to READ_SCOPE_VALUE_REF.
   */
  readonly scope?:
    | ReadScope
    | undefined;
  /**
   * One or more values to associate with this tag.
   *
   * Will be deduplicated on `value`, retaining the highest scope.
   */
  readonly values: readonly Tag_Value[];
}

/** A single value associated with a tag. */
export interface Tag_Value {
  /**
   * Scope indicates the level at which this *value* can be read.
   *
   * If unset, the value will be retained on the ValueRef, but will not be
   * indexed.
   *
   * Must be <= `Tag.scope`.
   *
   * Defaults to READ_SCOPE_VALUE_REF.
   */
  readonly scope?:
    | ReadScope
    | undefined;
  /**
   * Indicates how many times this value was duplicated.
   *
   * Unset/0 indicates that this value is unique.
   * Values > 0 indicate how many duplications occurred.
   *
   * e.g. if this tag had values like ["a", "b", "a"], the "a" Value would set
   * duplicate_count to `1` and the "b" Value would have `duplicate_count`
   * unset.
   */
  readonly duplicateCount?:
    | number
    | undefined;
  /** A string value. Indexed verbatim and supports equality searches. */
  readonly strValue?:
    | string
    | undefined;
  /** A boolean value. Indexed verbatim and supports equality searches. */
  readonly boolValue?:
    | boolean
    | undefined;
  /**
   * A numeric (integer) value. Indexed both for equality and inequality.
   *
   * NOTE: Numeric tags are not summed. If you have multiple tags:
   *   * failure_count=10
   *   * failure_count=8
   *   * failure_count=2
   *
   * And then query for failure_count>10, it will NOT be a match.
   */
  readonly intValue?: string | undefined;
}

function createBaseTag(): Tag {
  return { key: undefined, scope: undefined, values: [] };
}

export const Tag: MessageFns<Tag> = {
  encode(message: Tag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined) {
      writer.uint32(10).string(message.key);
    }
    if (message.scope !== undefined) {
      writer.uint32(16).int32(message.scope);
    }
    for (const v of message.values) {
      Tag_Value.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTag() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.scope = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.values.push(Tag_Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tag {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : undefined,
      scope: isSet(object.scope) ? readScopeFromJSON(object.scope) : undefined,
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => Tag_Value.fromJSON(e)) : [],
    };
  },

  toJSON(message: Tag): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    if (message.scope !== undefined) {
      obj.scope = readScopeToJSON(message.scope);
    }
    if (message.values?.length) {
      obj.values = message.values.map((e) => Tag_Value.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Tag>): Tag {
    return Tag.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Tag>): Tag {
    const message = createBaseTag() as any;
    message.key = object.key ?? undefined;
    message.scope = object.scope ?? undefined;
    message.values = object.values?.map((e) => Tag_Value.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTag_Value(): Tag_Value {
  return {
    scope: undefined,
    duplicateCount: undefined,
    strValue: undefined,
    boolValue: undefined,
    intValue: undefined,
  };
}

export const Tag_Value: MessageFns<Tag_Value> = {
  encode(message: Tag_Value, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scope !== undefined) {
      writer.uint32(8).int32(message.scope);
    }
    if (message.duplicateCount !== undefined) {
      writer.uint32(16).uint32(message.duplicateCount);
    }
    if (message.strValue !== undefined) {
      writer.uint32(26).string(message.strValue);
    }
    if (message.boolValue !== undefined) {
      writer.uint32(32).bool(message.boolValue);
    }
    if (message.intValue !== undefined) {
      writer.uint32(40).int64(message.intValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tag_Value {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTag_Value() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.scope = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.duplicateCount = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.strValue = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.boolValue = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.intValue = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tag_Value {
    return {
      scope: isSet(object.scope) ? readScopeFromJSON(object.scope) : undefined,
      duplicateCount: isSet(object.duplicateCount) ? globalThis.Number(object.duplicateCount) : undefined,
      strValue: isSet(object.strValue) ? globalThis.String(object.strValue) : undefined,
      boolValue: isSet(object.boolValue) ? globalThis.Boolean(object.boolValue) : undefined,
      intValue: isSet(object.intValue) ? globalThis.String(object.intValue) : undefined,
    };
  },

  toJSON(message: Tag_Value): unknown {
    const obj: any = {};
    if (message.scope !== undefined) {
      obj.scope = readScopeToJSON(message.scope);
    }
    if (message.duplicateCount !== undefined) {
      obj.duplicateCount = Math.round(message.duplicateCount);
    }
    if (message.strValue !== undefined) {
      obj.strValue = message.strValue;
    }
    if (message.boolValue !== undefined) {
      obj.boolValue = message.boolValue;
    }
    if (message.intValue !== undefined) {
      obj.intValue = message.intValue;
    }
    return obj;
  },

  create(base?: DeepPartial<Tag_Value>): Tag_Value {
    return Tag_Value.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Tag_Value>): Tag_Value {
    const message = createBaseTag_Value() as any;
    message.scope = object.scope ?? undefined;
    message.duplicateCount = object.duplicateCount ?? undefined;
    message.strValue = object.strValue ?? undefined;
    message.boolValue = object.boolValue ?? undefined;
    message.intValue = object.intValue ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

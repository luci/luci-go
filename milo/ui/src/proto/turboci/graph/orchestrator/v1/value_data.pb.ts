// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/value_data.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../google/protobuf/any.pb";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/**
 * ValueData holds actual data (via either binary or JSONPB encoded protobuf)
 * embedded inside of a TurboCI node (Checks/Stages) and referenced via
 * [ValueRef].
 *
 * If a [ValueRef] references a ValueData via `digest`, it will be returned to
 * the reader via a 'value_data' map which looks like:
 *
 *   map<string, ValueData> value_data;
 *
 * ValueData is written via [ValueWrite] - the orchestrator service will
 * transform these into [ValueRef] and ValueData, and choose via heruistics to
 * either store the ValueData inline in the [ValueRef] or referenced via digest.
 */
export interface ValueData {
  /** Binary encoded data. */
  readonly binary?:
    | Any
    | undefined;
  /** JSON encoded data. */
  readonly json?: ValueData_JsonAny | undefined;
}

/** JsonAny is like a `google.protobuf.Any`, except the value is encoded JSON. */
export interface ValueData_JsonAny {
  /**
   * The type_url of this data. Exactly the same form as
   * google.protobuf.Any.type_url.
   *
   * Will have the form `type.googleapis.com/proto.package.MessageName`.
   */
  readonly typeUrl?:
    | string
    | undefined;
  /** The value of the data encoded as JSONPB. */
  readonly value?:
    | string
    | undefined;
  /**
   * If true, the server was unable to fully encode the raw binary data, and
   * `value` above is missing unknown fields.
   */
  readonly hasUnknownFields?: boolean | undefined;
}

function createBaseValueData(): ValueData {
  return { binary: undefined, json: undefined };
}

export const ValueData: MessageFns<ValueData> = {
  encode(message: ValueData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.binary !== undefined) {
      Any.encode(message.binary, writer.uint32(10).fork()).join();
    }
    if (message.json !== undefined) {
      ValueData_JsonAny.encode(message.json, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValueData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValueData() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.binary = Any.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.json = ValueData_JsonAny.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValueData {
    return {
      binary: isSet(object.binary) ? Any.fromJSON(object.binary) : undefined,
      json: isSet(object.json) ? ValueData_JsonAny.fromJSON(object.json) : undefined,
    };
  },

  toJSON(message: ValueData): unknown {
    const obj: any = {};
    if (message.binary !== undefined) {
      obj.binary = Any.toJSON(message.binary);
    }
    if (message.json !== undefined) {
      obj.json = ValueData_JsonAny.toJSON(message.json);
    }
    return obj;
  },

  create(base?: DeepPartial<ValueData>): ValueData {
    return ValueData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ValueData>): ValueData {
    const message = createBaseValueData() as any;
    message.binary = (object.binary !== undefined && object.binary !== null)
      ? Any.fromPartial(object.binary)
      : undefined;
    message.json = (object.json !== undefined && object.json !== null)
      ? ValueData_JsonAny.fromPartial(object.json)
      : undefined;
    return message;
  },
};

function createBaseValueData_JsonAny(): ValueData_JsonAny {
  return { typeUrl: undefined, value: undefined, hasUnknownFields: undefined };
}

export const ValueData_JsonAny: MessageFns<ValueData_JsonAny> = {
  encode(message: ValueData_JsonAny, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.typeUrl !== undefined) {
      writer.uint32(10).string(message.typeUrl);
    }
    if (message.value !== undefined) {
      writer.uint32(18).string(message.value);
    }
    if (message.hasUnknownFields !== undefined) {
      writer.uint32(24).bool(message.hasUnknownFields);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValueData_JsonAny {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValueData_JsonAny() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.typeUrl = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hasUnknownFields = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValueData_JsonAny {
    return {
      typeUrl: isSet(object.typeUrl) ? globalThis.String(object.typeUrl) : undefined,
      value: isSet(object.value) ? globalThis.String(object.value) : undefined,
      hasUnknownFields: isSet(object.hasUnknownFields) ? globalThis.Boolean(object.hasUnknownFields) : undefined,
    };
  },

  toJSON(message: ValueData_JsonAny): unknown {
    const obj: any = {};
    if (message.typeUrl !== undefined) {
      obj.typeUrl = message.typeUrl;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    if (message.hasUnknownFields !== undefined) {
      obj.hasUnknownFields = message.hasUnknownFields;
    }
    return obj;
  },

  create(base?: DeepPartial<ValueData_JsonAny>): ValueData_JsonAny {
    return ValueData_JsonAny.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ValueData_JsonAny>): ValueData_JsonAny {
    const message = createBaseValueData_JsonAny() as any;
    message.typeUrl = object.typeUrl ?? undefined;
    message.value = object.value ?? undefined;
    message.hasUnknownFields = object.hasUnknownFields ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

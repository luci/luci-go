// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.32.1
// source: turboci/graph/orchestrator/v1/write_nodes_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Revision } from "./revision.pb";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/** Response message for TurboCIGraphService.WriteNodes. */
export interface WriteNodesResponse {
  /**
   * The committed revision of this write.
   *
   * This can be used to read back e.g. Edits from the graph or otherwise
   * communicate to other readers what version they must observe to see the
   * writes done in this RPC.
   *
   * You can, technically, chain Writes together with this - however if a Write
   * finds a transaction conflict, you will have to restart the chain from that
   * write (not from the beginning of the chain).
   *
   * See WriteNodesRequest.TransactionDetails.
   */
  readonly writtenVersion?: Revision | undefined;
}

function createBaseWriteNodesResponse(): WriteNodesResponse {
  return { writtenVersion: undefined };
}

export const WriteNodesResponse: MessageFns<WriteNodesResponse> = {
  encode(message: WriteNodesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.writtenVersion !== undefined) {
      Revision.encode(message.writtenVersion, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteNodesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteNodesResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.writtenVersion = Revision.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteNodesResponse {
    return { writtenVersion: isSet(object.writtenVersion) ? Revision.fromJSON(object.writtenVersion) : undefined };
  },

  toJSON(message: WriteNodesResponse): unknown {
    const obj: any = {};
    if (message.writtenVersion !== undefined) {
      obj.writtenVersion = Revision.toJSON(message.writtenVersion);
    }
    return obj;
  },

  create(base?: DeepPartial<WriteNodesResponse>): WriteNodesResponse {
    return WriteNodesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteNodesResponse>): WriteNodesResponse {
    const message = createBaseWriteNodesResponse() as any;
    message.writtenVersion = (object.writtenVersion !== undefined && object.writtenVersion !== null)
      ? Revision.fromPartial(object.writtenVersion)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

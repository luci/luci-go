// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: turboci/graph/orchestrator/v1/stage_delta.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { StageAttempt } from "../../ids/v1/identifier.pb";
import { Dependencies } from "./dependencies.pb";
import { StageAttemptState, stageAttemptStateFromJSON, stageAttemptStateToJSON } from "./stage_attempt_state.pb";
import { StageState, stageStateFromJSON, stageStateToJSON } from "./stage_state.pb";
import { Value } from "./value.pb";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/** Encapsulates data changes to a Stage object. */
export interface StageDelta {
  /** If set, the new Stage state that was set as part of this edit. */
  readonly state?:
    | StageState
    | undefined;
  /**
   * Attempt(s) which were modified as part of this edit.
   *
   * NOTE: This should have a maximum length of 2 when an old Attempt is made
   * INCOMPLETE at the same time a new Attempt is written as PENDING.
   */
  readonly attempts: readonly StageDelta_Attempt[];
  /** Cancelled is true if the edit cancelled this Stage. */
  readonly cancelled?:
    | boolean
    | undefined;
  /**
   * The Stage continuation_group written as part of this edit.
   *
   * The fields within reflect what was changed in this edit:
   *   * `edges`, `predicate` and `resolution` are included in-whole, and mean
   *     that this edit modified those fields.
   *   * `resolution_events` reflects just new events resolved in this edit.
   */
  readonly continuationGroup?: Dependencies | undefined;
}

/** Attempt represents a delta to a given StageAttempt. */
export interface StageDelta_Attempt {
  /** The identifier of the StageAttempt which was edited. */
  readonly identifier?:
    | StageAttempt
    | undefined;
  /** The state that the Attempt entered as part of this Edit. */
  readonly state?:
    | StageAttemptState
    | undefined;
  /**
   * Details written as part of this edit.
   *
   * NOTE: For now, the Value here will be devoid of content other than
   * `type_url`.
   */
  readonly details: readonly Value[];
  /** The index of the progress message(s) written as part of this edit. */
  readonly progress: readonly string[];
}

function createBaseStageDelta(): StageDelta {
  return { state: undefined, attempts: [], cancelled: undefined, continuationGroup: undefined };
}

export const StageDelta: MessageFns<StageDelta> = {
  encode(message: StageDelta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== undefined) {
      writer.uint32(8).int32(message.state);
    }
    for (const v of message.attempts) {
      StageDelta_Attempt.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.cancelled !== undefined) {
      writer.uint32(24).bool(message.cancelled);
    }
    if (message.continuationGroup !== undefined) {
      Dependencies.encode(message.continuationGroup, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StageDelta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStageDelta() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.attempts.push(StageDelta_Attempt.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.cancelled = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.continuationGroup = Dependencies.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StageDelta {
    return {
      state: isSet(object.state) ? stageStateFromJSON(object.state) : undefined,
      attempts: globalThis.Array.isArray(object?.attempts)
        ? object.attempts.map((e: any) => StageDelta_Attempt.fromJSON(e))
        : [],
      cancelled: isSet(object.cancelled) ? globalThis.Boolean(object.cancelled) : undefined,
      continuationGroup: isSet(object.continuationGroup) ? Dependencies.fromJSON(object.continuationGroup) : undefined,
    };
  },

  toJSON(message: StageDelta): unknown {
    const obj: any = {};
    if (message.state !== undefined) {
      obj.state = stageStateToJSON(message.state);
    }
    if (message.attempts?.length) {
      obj.attempts = message.attempts.map((e) => StageDelta_Attempt.toJSON(e));
    }
    if (message.cancelled !== undefined) {
      obj.cancelled = message.cancelled;
    }
    if (message.continuationGroup !== undefined) {
      obj.continuationGroup = Dependencies.toJSON(message.continuationGroup);
    }
    return obj;
  },

  create(base?: DeepPartial<StageDelta>): StageDelta {
    return StageDelta.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StageDelta>): StageDelta {
    const message = createBaseStageDelta() as any;
    message.state = object.state ?? undefined;
    message.attempts = object.attempts?.map((e) => StageDelta_Attempt.fromPartial(e)) || [];
    message.cancelled = object.cancelled ?? undefined;
    message.continuationGroup = (object.continuationGroup !== undefined && object.continuationGroup !== null)
      ? Dependencies.fromPartial(object.continuationGroup)
      : undefined;
    return message;
  },
};

function createBaseStageDelta_Attempt(): StageDelta_Attempt {
  return { identifier: undefined, state: undefined, details: [], progress: [] };
}

export const StageDelta_Attempt: MessageFns<StageDelta_Attempt> = {
  encode(message: StageDelta_Attempt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.identifier !== undefined) {
      StageAttempt.encode(message.identifier, writer.uint32(10).fork()).join();
    }
    if (message.state !== undefined) {
      writer.uint32(16).int32(message.state);
    }
    for (const v of message.details) {
      Value.encode(v!, writer.uint32(26).fork()).join();
    }
    writer.uint32(34).fork();
    for (const v of message.progress) {
      writer.int64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StageDelta_Attempt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStageDelta_Attempt() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.identifier = StageAttempt.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.details.push(Value.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.progress.push(reader.int64().toString());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.progress.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StageDelta_Attempt {
    return {
      identifier: isSet(object.identifier) ? StageAttempt.fromJSON(object.identifier) : undefined,
      state: isSet(object.state) ? stageAttemptStateFromJSON(object.state) : undefined,
      details: globalThis.Array.isArray(object?.details) ? object.details.map((e: any) => Value.fromJSON(e)) : [],
      progress: globalThis.Array.isArray(object?.progress) ? object.progress.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: StageDelta_Attempt): unknown {
    const obj: any = {};
    if (message.identifier !== undefined) {
      obj.identifier = StageAttempt.toJSON(message.identifier);
    }
    if (message.state !== undefined) {
      obj.state = stageAttemptStateToJSON(message.state);
    }
    if (message.details?.length) {
      obj.details = message.details.map((e) => Value.toJSON(e));
    }
    if (message.progress?.length) {
      obj.progress = message.progress;
    }
    return obj;
  },

  create(base?: DeepPartial<StageDelta_Attempt>): StageDelta_Attempt {
    return StageDelta_Attempt.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StageDelta_Attempt>): StageDelta_Attempt {
    const message = createBaseStageDelta_Attempt() as any;
    message.identifier = (object.identifier !== undefined && object.identifier !== null)
      ? StageAttempt.fromPartial(object.identifier)
      : undefined;
    message.state = object.state ?? undefined;
    message.details = object.details?.map((e) => Value.fromPartial(e)) || [];
    message.progress = object.progress?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

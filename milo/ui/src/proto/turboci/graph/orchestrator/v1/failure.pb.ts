// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.32.1
// source: turboci/graph/orchestrator/v1/failure.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "turboci.graph.orchestrator.v1";

/**
 * Failure is for stage attempt execution failure information.
 *
 * Note: this message is not intended to be used for workflow-level failures.
 * For example if a stage attempt finishes everything it is intended to do
 * (i.e. to run a suite of tests), and it produces some failure
 * results (i.e. some tests failed), those results should be reported to the
 * corresponding check's result.Data, not here.
 */
export interface Failure {
  /** Human-readable error message. */
  readonly message?:
    | string
    | undefined;
  /** Failure to transition a stage attempt to SCHEDULED state. */
  readonly toScheduled?:
    | ToScheduled
    | undefined;
  /** Failure to transition a stage attempt to RUNNING state. */
  readonly toRunning?: ToRunning | undefined;
}

/**
 * Failure to transition a stage attempt to SCHEDULED state.
 * Can be used by:
 * * the Stage Executor to update a stage attempt if it fails to schedule the
 *   execution for it.
 * * the Orchestrator to update
 *   * a stage attempt if it exceeds its execution_policy.Timeout.PendingThrottled.
 *   * a check's result from this failed stage attempt.
 */
export interface ToScheduled {
}

/**
 * Failure to transition a stage attempt to RUNNING state.
 * Can be used by:
 * * the Stage Executor to update a stage attempt if it fails to execute.
 * * the Orchestrator to update
 *   * a stage attempt if it exceeds its execution_policy.Timeout.Scheduled.
 *   * a check's result from this failed stage attempt.
 */
export interface ToRunning {
}

function createBaseFailure(): Failure {
  return { message: undefined, toScheduled: undefined, toRunning: undefined };
}

export const Failure: MessageFns<Failure> = {
  encode(message: Failure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== undefined) {
      writer.uint32(10).string(message.message);
    }
    if (message.toScheduled !== undefined) {
      ToScheduled.encode(message.toScheduled, writer.uint32(18).fork()).join();
    }
    if (message.toRunning !== undefined) {
      ToRunning.encode(message.toRunning, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Failure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFailure() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toScheduled = ToScheduled.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toRunning = ToRunning.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Failure {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
      toScheduled: isSet(object.toScheduled) ? ToScheduled.fromJSON(object.toScheduled) : undefined,
      toRunning: isSet(object.toRunning) ? ToRunning.fromJSON(object.toRunning) : undefined,
    };
  },

  toJSON(message: Failure): unknown {
    const obj: any = {};
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    if (message.toScheduled !== undefined) {
      obj.toScheduled = ToScheduled.toJSON(message.toScheduled);
    }
    if (message.toRunning !== undefined) {
      obj.toRunning = ToRunning.toJSON(message.toRunning);
    }
    return obj;
  },

  create(base?: DeepPartial<Failure>): Failure {
    return Failure.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Failure>): Failure {
    const message = createBaseFailure() as any;
    message.message = object.message ?? undefined;
    message.toScheduled = (object.toScheduled !== undefined && object.toScheduled !== null)
      ? ToScheduled.fromPartial(object.toScheduled)
      : undefined;
    message.toRunning = (object.toRunning !== undefined && object.toRunning !== null)
      ? ToRunning.fromPartial(object.toRunning)
      : undefined;
    return message;
  },
};

function createBaseToScheduled(): ToScheduled {
  return {};
}

export const ToScheduled: MessageFns<ToScheduled> = {
  encode(_: ToScheduled, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToScheduled {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToScheduled() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ToScheduled {
    return {};
  },

  toJSON(_: ToScheduled): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ToScheduled>): ToScheduled {
    return ToScheduled.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ToScheduled>): ToScheduled {
    const message = createBaseToScheduled() as any;
    return message;
  },
};

function createBaseToRunning(): ToRunning {
  return {};
}

export const ToRunning: MessageFns<ToRunning> = {
  encode(_: ToRunning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToRunning {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToRunning() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ToRunning {
    return {};
  },

  toJSON(_: ToRunning): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ToRunning>): ToRunning {
    return ToRunning.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ToRunning>): ToRunning {
    const message = createBaseToRunning() as any;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

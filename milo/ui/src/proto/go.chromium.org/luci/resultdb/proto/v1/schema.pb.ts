// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: go.chromium.org/luci/resultdb/proto/v1/schema.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "luci.resultdb.v1";

export interface GetSchemaRequest {
  /**
   * The resource name of the schema to retrieve.
   * Note: This is currently a https://google.aip.dev/156 singleton resource, so
   * the only valid resource name is "schema".
   */
  readonly name: string;
}

/** Schema contains information about the schema used for test results. */
export interface Schema {
  /**
   * The resource name of the schema.
   * Currently, this is always "schema".
   */
  readonly name: string;
  /** The schemes available for use in test identifiers, by scheme identifier. */
  readonly schemes: { [key: string]: Scheme };
}

export interface Schema_SchemesEntry {
  readonly key: string;
  readonly value: Scheme | undefined;
}

export interface GetSchemeRequest {
  /**
   * The resource name of the scheme to retrieve.
   * Format: schema/schemes/{scheme_id}.
   */
  readonly name: string;
}

/**
 * A scheme represents a kind of test type. For example, a JUnit tests
 * or Google Tests. Schemes control how tests with that type are
 * presented on the UI.
 *
 * Tests are associated with a type at the module level, via the module
 * type field.
 *
 * Schemes are ResultDB deployment-level configuration.
 *
 * Next id: 6.
 */
export interface Scheme {
  /**
   * The resource name of the scheme.
   * Format: schema/schemes/{id}.
   */
  readonly name: string;
  /**
   * The identifier for the scheme, e.g. 'junit'.
   *
   * Limited to ^[a-z][a-z0-9]{0,19}$.
   */
  readonly id: string;
  /**
   * A human readable name for the scheme, describing the test type.
   * For example, "JUnit" or "Web Tests".
   *
   * Please pay attention to capitalisation (should be similar to examples above)
   * and avoid any punctuation.
   */
  readonly humanReadableName: string;
  /**
   * The coarse level in the test hierarchy. Optional.
   *
   * If it is set, this level is enabled and a value for this hierarchy level must be set
   * for all test results using this scheme.
   * If it is not set, a value for this hierarchy level must NOT be set for test results
   * using this scheme.
   *
   * Enabling or disabling a hierarchy level after it has been created is not permitted unless
   * no data has been uploaded for the scheme.
   *
   * If only one of coarse and fine hierarchy should be enabled, enable the fine hierarchy
   * only.
   */
  readonly coarse:
    | Scheme_Level
    | undefined;
  /**
   * The fine level in the test hierarchy. Optional.
   *
   * If it is set, this level is enabled and a value for this hierarchy level must be set
   * for all test results using this scheme.
   * If it is not set, a value for this hierarchy level must NOT be set for test results
   * using this scheme.
   *
   * Enabling or disabling a hierarchy level on a scheme after it is already being used
   * will result in existing uploads breaking and break presentation of already uploaded
   * data. Please use extreme caution if doing this; generally, it is better to define
   * a new scheme instead.
   */
  readonly fine:
    | Scheme_Level
    | undefined;
  /** The case level in the test hierarchy. This is the finest level. Required. */
  readonly case: Scheme_Level | undefined;
}

/** Configuration for a level of test hierarchy. */
export interface Scheme_Level {
  /**
   * The human readable name for the hierarchy level, as it should appear on the UI.
   * For example, "Package", "Class" or "Method".
   *
   * Please pay attention to capitalisation (should be similar to examples above)
   * and avoid any punctuation.
   *
   * Required.
   */
  readonly humanReadableName: string;
  /**
   * The regexp that defines valid values for this field. The value here will be
   * wrapped in ^...$. Validation will apply to all newly uploaded test results.
   * Use RE2 syntax.
   *
   * If blank, all values are taken to be valid.
   *
   * Please take care changing this value, as uploads may start to fail.
   */
  readonly validationRegexp: string;
}

function createBaseGetSchemaRequest(): GetSchemaRequest {
  return { name: "" };
}

export const GetSchemaRequest: MessageFns<GetSchemaRequest> = {
  encode(message: GetSchemaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSchemaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSchemaRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSchemaRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetSchemaRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<GetSchemaRequest>): GetSchemaRequest {
    return GetSchemaRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSchemaRequest>): GetSchemaRequest {
    const message = createBaseGetSchemaRequest() as any;
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseSchema(): Schema {
  return { name: "", schemes: {} };
}

export const Schema: MessageFns<Schema> = {
  encode(message: Schema, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    Object.entries(message.schemes).forEach(([key, value]) => {
      Schema_SchemesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Schema {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchema() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = Schema_SchemesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.schemes[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Schema {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      schemes: isObject(object.schemes)
        ? Object.entries(object.schemes).reduce<{ [key: string]: Scheme }>((acc, [key, value]) => {
          acc[key] = Scheme.fromJSON(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: Schema): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.schemes) {
      const entries = Object.entries(message.schemes);
      if (entries.length > 0) {
        obj.schemes = {};
        entries.forEach(([k, v]) => {
          obj.schemes[k] = Scheme.toJSON(v);
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<Schema>): Schema {
    return Schema.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Schema>): Schema {
    const message = createBaseSchema() as any;
    message.name = object.name ?? "";
    message.schemes = Object.entries(object.schemes ?? {}).reduce<{ [key: string]: Scheme }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = Scheme.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseSchema_SchemesEntry(): Schema_SchemesEntry {
  return { key: "", value: undefined };
}

export const Schema_SchemesEntry: MessageFns<Schema_SchemesEntry> = {
  encode(message: Schema_SchemesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Scheme.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Schema_SchemesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchema_SchemesEntry() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Scheme.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Schema_SchemesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Scheme.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: Schema_SchemesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Scheme.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<Schema_SchemesEntry>): Schema_SchemesEntry {
    return Schema_SchemesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Schema_SchemesEntry>): Schema_SchemesEntry {
    const message = createBaseSchema_SchemesEntry() as any;
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? Scheme.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseGetSchemeRequest(): GetSchemeRequest {
  return { name: "" };
}

export const GetSchemeRequest: MessageFns<GetSchemeRequest> = {
  encode(message: GetSchemeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSchemeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSchemeRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSchemeRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetSchemeRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<GetSchemeRequest>): GetSchemeRequest {
    return GetSchemeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSchemeRequest>): GetSchemeRequest {
    const message = createBaseGetSchemeRequest() as any;
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseScheme(): Scheme {
  return { name: "", id: "", humanReadableName: "", coarse: undefined, fine: undefined, case: undefined };
}

export const Scheme: MessageFns<Scheme> = {
  encode(message: Scheme, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.humanReadableName !== "") {
      writer.uint32(26).string(message.humanReadableName);
    }
    if (message.coarse !== undefined) {
      Scheme_Level.encode(message.coarse, writer.uint32(34).fork()).join();
    }
    if (message.fine !== undefined) {
      Scheme_Level.encode(message.fine, writer.uint32(42).fork()).join();
    }
    if (message.case !== undefined) {
      Scheme_Level.encode(message.case, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Scheme {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheme() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.humanReadableName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.coarse = Scheme_Level.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fine = Scheme_Level.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.case = Scheme_Level.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Scheme {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      humanReadableName: isSet(object.humanReadableName) ? globalThis.String(object.humanReadableName) : "",
      coarse: isSet(object.coarse) ? Scheme_Level.fromJSON(object.coarse) : undefined,
      fine: isSet(object.fine) ? Scheme_Level.fromJSON(object.fine) : undefined,
      case: isSet(object.case) ? Scheme_Level.fromJSON(object.case) : undefined,
    };
  },

  toJSON(message: Scheme): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.humanReadableName !== "") {
      obj.humanReadableName = message.humanReadableName;
    }
    if (message.coarse !== undefined) {
      obj.coarse = Scheme_Level.toJSON(message.coarse);
    }
    if (message.fine !== undefined) {
      obj.fine = Scheme_Level.toJSON(message.fine);
    }
    if (message.case !== undefined) {
      obj.case = Scheme_Level.toJSON(message.case);
    }
    return obj;
  },

  create(base?: DeepPartial<Scheme>): Scheme {
    return Scheme.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Scheme>): Scheme {
    const message = createBaseScheme() as any;
    message.name = object.name ?? "";
    message.id = object.id ?? "";
    message.humanReadableName = object.humanReadableName ?? "";
    message.coarse = (object.coarse !== undefined && object.coarse !== null)
      ? Scheme_Level.fromPartial(object.coarse)
      : undefined;
    message.fine = (object.fine !== undefined && object.fine !== null)
      ? Scheme_Level.fromPartial(object.fine)
      : undefined;
    message.case = (object.case !== undefined && object.case !== null)
      ? Scheme_Level.fromPartial(object.case)
      : undefined;
    return message;
  },
};

function createBaseScheme_Level(): Scheme_Level {
  return { humanReadableName: "", validationRegexp: "" };
}

export const Scheme_Level: MessageFns<Scheme_Level> = {
  encode(message: Scheme_Level, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.humanReadableName !== "") {
      writer.uint32(10).string(message.humanReadableName);
    }
    if (message.validationRegexp !== "") {
      writer.uint32(18).string(message.validationRegexp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Scheme_Level {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheme_Level() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.humanReadableName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.validationRegexp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Scheme_Level {
    return {
      humanReadableName: isSet(object.humanReadableName) ? globalThis.String(object.humanReadableName) : "",
      validationRegexp: isSet(object.validationRegexp) ? globalThis.String(object.validationRegexp) : "",
    };
  },

  toJSON(message: Scheme_Level): unknown {
    const obj: any = {};
    if (message.humanReadableName !== "") {
      obj.humanReadableName = message.humanReadableName;
    }
    if (message.validationRegexp !== "") {
      obj.validationRegexp = message.validationRegexp;
    }
    return obj;
  },

  create(base?: DeepPartial<Scheme_Level>): Scheme_Level {
    return Scheme_Level.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Scheme_Level>): Scheme_Level {
    const message = createBaseScheme_Level() as any;
    message.humanReadableName = object.humanReadableName ?? "";
    message.validationRegexp = object.validationRegexp ?? "";
    return message;
  },
};

/**
 * Service to read test schema.
 *
 * Use of LUCI is subject to the Google [Terms of Service](https://policies.google.com/terms)
 * and [Privacy Policy](https://policies.google.com/privacy).
 */
export interface Schemas {
  /** Reads information about the test result schema used on this ResultDB deployment. */
  Get(request: GetSchemaRequest): Promise<Schema>;
  /** Reads information about a particular test scheme. */
  GetScheme(request: GetSchemeRequest): Promise<Scheme>;
}

export const SchemasServiceName = "luci.resultdb.v1.Schemas";
export class SchemasClientImpl implements Schemas {
  static readonly DEFAULT_SERVICE = SchemasServiceName;
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || SchemasServiceName;
    this.rpc = rpc;
    this.Get = this.Get.bind(this);
    this.GetScheme = this.GetScheme.bind(this);
  }
  Get(request: GetSchemaRequest): Promise<Schema> {
    const data = GetSchemaRequest.toJSON(request);
    const promise = this.rpc.request(this.service, "Get", data);
    return promise.then((data) => Schema.fromJSON(data));
  }

  GetScheme(request: GetSchemeRequest): Promise<Scheme> {
    const data = GetSchemeRequest.toJSON(request);
    const promise = this.rpc.request(this.service, "GetScheme", data);
    return promise.then((data) => Scheme.fromJSON(data));
  }
}

interface Rpc {
  request(service: string, method: string, data: unknown): Promise<unknown>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

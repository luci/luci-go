// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: go.chromium.org/luci/crystal_ball/api/perf_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";
import { Struct } from "../../../../google/protobuf/struct.pb";
import { Status } from "../../../../google/rpc/status.pb";

export const protobufPackage = "google.android.perf.v1";

/** Defines the request message for the `StreamDashboardData` method. */
export interface StreamDashboardDataRequest {
  /** The resource name of the DashboardState to fetch data for. */
  readonly name: string;
  /** The core content of the dashboard to fetch data for. */
  readonly dashboardContent: PerfDashboardContent | undefined;
}

/** Defines the response message for the `StreamDashboardData` method. */
export interface StreamDashboardDataResponse {
  /** The ID of the widget this data result corresponds to. */
  readonly widgetId: string;
  /** Data specifically prepared for a MultiMetricChart. */
  readonly multiMetricChartData?:
    | MultiMetricChartData
    | undefined;
  /** Data for a Markdown widget */
  readonly markdownWidget?: MarkdownWidget | undefined;
  readonly status: Status | undefined;
}

/** Data structured and transformed for the MultiMetricChart component. */
export interface MultiMetricChartData {
  readonly xAxisDataKey: string;
  readonly yAxisDataKey: string;
  readonly lines: readonly MultiMetricLine[];
}

/** Represents a single line series in a MultiMetricChart. */
export interface MultiMetricLine {
  readonly seriesId: string;
  readonly legendLabel: string;
  readonly dataPoints: readonly { readonly [key: string]: any }[];
  readonly metricField: string;
}

/** Widget for displaying formatted text using Markdown. */
export interface MarkdownWidget {
  readonly content: string;
}

/**
 * Represents the content of a dashboard state.
 * NOTE: This is a minimal version. Add fields as needed by your StreamDashboardDataRequest.
 */
export interface PerfDashboardContent {
  readonly dataSpecs: { [key: string]: PerfDataSpec };
  readonly widgets: readonly PerfWidget[];
}

export interface PerfDashboardContent_DataSpecsEntry {
  readonly key: string;
  readonly value: PerfDataSpec | undefined;
}

export interface PerfDataSpec {
  /** Add other fields if used in the request */
  readonly displayName: string;
}

export interface PerfWidget {
  readonly id: string;
  readonly markdown?: MarkdownWidget | undefined;
  readonly chart?: PerfChartWidget | undefined;
}

export interface PerfChartWidget {
  /** Add other fields if used */
  readonly dataSpecId: string;
}

function createBaseStreamDashboardDataRequest(): StreamDashboardDataRequest {
  return { name: "", dashboardContent: undefined };
}

export const StreamDashboardDataRequest: MessageFns<StreamDashboardDataRequest> = {
  encode(message: StreamDashboardDataRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.dashboardContent !== undefined) {
      PerfDashboardContent.encode(message.dashboardContent, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamDashboardDataRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamDashboardDataRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dashboardContent = PerfDashboardContent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamDashboardDataRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      dashboardContent: isSet(object.dashboardContent)
        ? PerfDashboardContent.fromJSON(object.dashboardContent)
        : undefined,
    };
  },

  toJSON(message: StreamDashboardDataRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.dashboardContent !== undefined) {
      obj.dashboardContent = PerfDashboardContent.toJSON(message.dashboardContent);
    }
    return obj;
  },

  create(base?: DeepPartial<StreamDashboardDataRequest>): StreamDashboardDataRequest {
    return StreamDashboardDataRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamDashboardDataRequest>): StreamDashboardDataRequest {
    const message = createBaseStreamDashboardDataRequest() as any;
    message.name = object.name ?? "";
    message.dashboardContent = (object.dashboardContent !== undefined && object.dashboardContent !== null)
      ? PerfDashboardContent.fromPartial(object.dashboardContent)
      : undefined;
    return message;
  },
};

function createBaseStreamDashboardDataResponse(): StreamDashboardDataResponse {
  return { widgetId: "", multiMetricChartData: undefined, markdownWidget: undefined, status: undefined };
}

export const StreamDashboardDataResponse: MessageFns<StreamDashboardDataResponse> = {
  encode(message: StreamDashboardDataResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.widgetId !== "") {
      writer.uint32(10).string(message.widgetId);
    }
    if (message.multiMetricChartData !== undefined) {
      MultiMetricChartData.encode(message.multiMetricChartData, writer.uint32(18).fork()).join();
    }
    if (message.markdownWidget !== undefined) {
      MarkdownWidget.encode(message.markdownWidget, writer.uint32(34).fork()).join();
    }
    if (message.status !== undefined) {
      Status.encode(message.status, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamDashboardDataResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamDashboardDataResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.widgetId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.multiMetricChartData = MultiMetricChartData.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.markdownWidget = MarkdownWidget.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = Status.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamDashboardDataResponse {
    return {
      widgetId: isSet(object.widgetId) ? globalThis.String(object.widgetId) : "",
      multiMetricChartData: isSet(object.multiMetricChartData)
        ? MultiMetricChartData.fromJSON(object.multiMetricChartData)
        : undefined,
      markdownWidget: isSet(object.markdownWidget) ? MarkdownWidget.fromJSON(object.markdownWidget) : undefined,
      status: isSet(object.status) ? Status.fromJSON(object.status) : undefined,
    };
  },

  toJSON(message: StreamDashboardDataResponse): unknown {
    const obj: any = {};
    if (message.widgetId !== "") {
      obj.widgetId = message.widgetId;
    }
    if (message.multiMetricChartData !== undefined) {
      obj.multiMetricChartData = MultiMetricChartData.toJSON(message.multiMetricChartData);
    }
    if (message.markdownWidget !== undefined) {
      obj.markdownWidget = MarkdownWidget.toJSON(message.markdownWidget);
    }
    if (message.status !== undefined) {
      obj.status = Status.toJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<StreamDashboardDataResponse>): StreamDashboardDataResponse {
    return StreamDashboardDataResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StreamDashboardDataResponse>): StreamDashboardDataResponse {
    const message = createBaseStreamDashboardDataResponse() as any;
    message.widgetId = object.widgetId ?? "";
    message.multiMetricChartData = (object.multiMetricChartData !== undefined && object.multiMetricChartData !== null)
      ? MultiMetricChartData.fromPartial(object.multiMetricChartData)
      : undefined;
    message.markdownWidget = (object.markdownWidget !== undefined && object.markdownWidget !== null)
      ? MarkdownWidget.fromPartial(object.markdownWidget)
      : undefined;
    message.status = (object.status !== undefined && object.status !== null)
      ? Status.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseMultiMetricChartData(): MultiMetricChartData {
  return { xAxisDataKey: "", yAxisDataKey: "", lines: [] };
}

export const MultiMetricChartData: MessageFns<MultiMetricChartData> = {
  encode(message: MultiMetricChartData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.xAxisDataKey !== "") {
      writer.uint32(10).string(message.xAxisDataKey);
    }
    if (message.yAxisDataKey !== "") {
      writer.uint32(18).string(message.yAxisDataKey);
    }
    for (const v of message.lines) {
      MultiMetricLine.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultiMetricChartData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultiMetricChartData() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.xAxisDataKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.yAxisDataKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lines.push(MultiMetricLine.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultiMetricChartData {
    return {
      xAxisDataKey: isSet(object.xAxisDataKey) ? globalThis.String(object.xAxisDataKey) : "",
      yAxisDataKey: isSet(object.yAxisDataKey) ? globalThis.String(object.yAxisDataKey) : "",
      lines: globalThis.Array.isArray(object?.lines) ? object.lines.map((e: any) => MultiMetricLine.fromJSON(e)) : [],
    };
  },

  toJSON(message: MultiMetricChartData): unknown {
    const obj: any = {};
    if (message.xAxisDataKey !== "") {
      obj.xAxisDataKey = message.xAxisDataKey;
    }
    if (message.yAxisDataKey !== "") {
      obj.yAxisDataKey = message.yAxisDataKey;
    }
    if (message.lines?.length) {
      obj.lines = message.lines.map((e) => MultiMetricLine.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MultiMetricChartData>): MultiMetricChartData {
    return MultiMetricChartData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MultiMetricChartData>): MultiMetricChartData {
    const message = createBaseMultiMetricChartData() as any;
    message.xAxisDataKey = object.xAxisDataKey ?? "";
    message.yAxisDataKey = object.yAxisDataKey ?? "";
    message.lines = object.lines?.map((e) => MultiMetricLine.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMultiMetricLine(): MultiMetricLine {
  return { seriesId: "", legendLabel: "", dataPoints: [], metricField: "" };
}

export const MultiMetricLine: MessageFns<MultiMetricLine> = {
  encode(message: MultiMetricLine, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seriesId !== "") {
      writer.uint32(10).string(message.seriesId);
    }
    if (message.legendLabel !== "") {
      writer.uint32(18).string(message.legendLabel);
    }
    for (const v of message.dataPoints) {
      Struct.encode(Struct.wrap(v!), writer.uint32(26).fork()).join();
    }
    if (message.metricField !== "") {
      writer.uint32(34).string(message.metricField);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultiMetricLine {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultiMetricLine() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.seriesId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.legendLabel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dataPoints.push(Struct.unwrap(Struct.decode(reader, reader.uint32())));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.metricField = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultiMetricLine {
    return {
      seriesId: isSet(object.seriesId) ? globalThis.String(object.seriesId) : "",
      legendLabel: isSet(object.legendLabel) ? globalThis.String(object.legendLabel) : "",
      dataPoints: globalThis.Array.isArray(object?.dataPoints) ? [...object.dataPoints] : [],
      metricField: isSet(object.metricField) ? globalThis.String(object.metricField) : "",
    };
  },

  toJSON(message: MultiMetricLine): unknown {
    const obj: any = {};
    if (message.seriesId !== "") {
      obj.seriesId = message.seriesId;
    }
    if (message.legendLabel !== "") {
      obj.legendLabel = message.legendLabel;
    }
    if (message.dataPoints?.length) {
      obj.dataPoints = message.dataPoints;
    }
    if (message.metricField !== "") {
      obj.metricField = message.metricField;
    }
    return obj;
  },

  create(base?: DeepPartial<MultiMetricLine>): MultiMetricLine {
    return MultiMetricLine.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MultiMetricLine>): MultiMetricLine {
    const message = createBaseMultiMetricLine() as any;
    message.seriesId = object.seriesId ?? "";
    message.legendLabel = object.legendLabel ?? "";
    message.dataPoints = object.dataPoints?.map((e) => e) || [];
    message.metricField = object.metricField ?? "";
    return message;
  },
};

function createBaseMarkdownWidget(): MarkdownWidget {
  return { content: "" };
}

export const MarkdownWidget: MessageFns<MarkdownWidget> = {
  encode(message: MarkdownWidget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarkdownWidget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarkdownWidget() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarkdownWidget {
    return { content: isSet(object.content) ? globalThis.String(object.content) : "" };
  },

  toJSON(message: MarkdownWidget): unknown {
    const obj: any = {};
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<MarkdownWidget>): MarkdownWidget {
    return MarkdownWidget.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MarkdownWidget>): MarkdownWidget {
    const message = createBaseMarkdownWidget() as any;
    message.content = object.content ?? "";
    return message;
  },
};

function createBasePerfDashboardContent(): PerfDashboardContent {
  return { dataSpecs: {}, widgets: [] };
}

export const PerfDashboardContent: MessageFns<PerfDashboardContent> = {
  encode(message: PerfDashboardContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    Object.entries(message.dataSpecs).forEach(([key, value]) => {
      PerfDashboardContent_DataSpecsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    for (const v of message.widgets) {
      PerfWidget.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PerfDashboardContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePerfDashboardContent() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = PerfDashboardContent_DataSpecsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.dataSpecs[entry1.key] = entry1.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.widgets.push(PerfWidget.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PerfDashboardContent {
    return {
      dataSpecs: isObject(object.dataSpecs)
        ? Object.entries(object.dataSpecs).reduce<{ [key: string]: PerfDataSpec }>((acc, [key, value]) => {
          acc[key] = PerfDataSpec.fromJSON(value);
          return acc;
        }, {})
        : {},
      widgets: globalThis.Array.isArray(object?.widgets) ? object.widgets.map((e: any) => PerfWidget.fromJSON(e)) : [],
    };
  },

  toJSON(message: PerfDashboardContent): unknown {
    const obj: any = {};
    if (message.dataSpecs) {
      const entries = Object.entries(message.dataSpecs);
      if (entries.length > 0) {
        obj.dataSpecs = {};
        entries.forEach(([k, v]) => {
          obj.dataSpecs[k] = PerfDataSpec.toJSON(v);
        });
      }
    }
    if (message.widgets?.length) {
      obj.widgets = message.widgets.map((e) => PerfWidget.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PerfDashboardContent>): PerfDashboardContent {
    return PerfDashboardContent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PerfDashboardContent>): PerfDashboardContent {
    const message = createBasePerfDashboardContent() as any;
    message.dataSpecs = Object.entries(object.dataSpecs ?? {}).reduce<{ [key: string]: PerfDataSpec }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = PerfDataSpec.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.widgets = object.widgets?.map((e) => PerfWidget.fromPartial(e)) || [];
    return message;
  },
};

function createBasePerfDashboardContent_DataSpecsEntry(): PerfDashboardContent_DataSpecsEntry {
  return { key: "", value: undefined };
}

export const PerfDashboardContent_DataSpecsEntry: MessageFns<PerfDashboardContent_DataSpecsEntry> = {
  encode(message: PerfDashboardContent_DataSpecsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      PerfDataSpec.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PerfDashboardContent_DataSpecsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePerfDashboardContent_DataSpecsEntry() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = PerfDataSpec.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PerfDashboardContent_DataSpecsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? PerfDataSpec.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: PerfDashboardContent_DataSpecsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = PerfDataSpec.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<PerfDashboardContent_DataSpecsEntry>): PerfDashboardContent_DataSpecsEntry {
    return PerfDashboardContent_DataSpecsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PerfDashboardContent_DataSpecsEntry>): PerfDashboardContent_DataSpecsEntry {
    const message = createBasePerfDashboardContent_DataSpecsEntry() as any;
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? PerfDataSpec.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBasePerfDataSpec(): PerfDataSpec {
  return { displayName: "" };
}

export const PerfDataSpec: MessageFns<PerfDataSpec> = {
  encode(message: PerfDataSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.displayName !== "") {
      writer.uint32(10).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PerfDataSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePerfDataSpec() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PerfDataSpec {
    return { displayName: isSet(object.displayName) ? globalThis.String(object.displayName) : "" };
  },

  toJSON(message: PerfDataSpec): unknown {
    const obj: any = {};
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create(base?: DeepPartial<PerfDataSpec>): PerfDataSpec {
    return PerfDataSpec.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PerfDataSpec>): PerfDataSpec {
    const message = createBasePerfDataSpec() as any;
    message.displayName = object.displayName ?? "";
    return message;
  },
};

function createBasePerfWidget(): PerfWidget {
  return { id: "", markdown: undefined, chart: undefined };
}

export const PerfWidget: MessageFns<PerfWidget> = {
  encode(message: PerfWidget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.markdown !== undefined) {
      MarkdownWidget.encode(message.markdown, writer.uint32(26).fork()).join();
    }
    if (message.chart !== undefined) {
      PerfChartWidget.encode(message.chart, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PerfWidget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePerfWidget() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.markdown = MarkdownWidget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.chart = PerfChartWidget.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PerfWidget {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      markdown: isSet(object.markdown) ? MarkdownWidget.fromJSON(object.markdown) : undefined,
      chart: isSet(object.chart) ? PerfChartWidget.fromJSON(object.chart) : undefined,
    };
  },

  toJSON(message: PerfWidget): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.markdown !== undefined) {
      obj.markdown = MarkdownWidget.toJSON(message.markdown);
    }
    if (message.chart !== undefined) {
      obj.chart = PerfChartWidget.toJSON(message.chart);
    }
    return obj;
  },

  create(base?: DeepPartial<PerfWidget>): PerfWidget {
    return PerfWidget.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PerfWidget>): PerfWidget {
    const message = createBasePerfWidget() as any;
    message.id = object.id ?? "";
    message.markdown = (object.markdown !== undefined && object.markdown !== null)
      ? MarkdownWidget.fromPartial(object.markdown)
      : undefined;
    message.chart = (object.chart !== undefined && object.chart !== null)
      ? PerfChartWidget.fromPartial(object.chart)
      : undefined;
    return message;
  },
};

function createBasePerfChartWidget(): PerfChartWidget {
  return { dataSpecId: "" };
}

export const PerfChartWidget: MessageFns<PerfChartWidget> = {
  encode(message: PerfChartWidget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dataSpecId !== "") {
      writer.uint32(10).string(message.dataSpecId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PerfChartWidget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePerfChartWidget() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dataSpecId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PerfChartWidget {
    return { dataSpecId: isSet(object.dataSpecId) ? globalThis.String(object.dataSpecId) : "" };
  },

  toJSON(message: PerfChartWidget): unknown {
    const obj: any = {};
    if (message.dataSpecId !== "") {
      obj.dataSpecId = message.dataSpecId;
    }
    return obj;
  },

  create(base?: DeepPartial<PerfChartWidget>): PerfChartWidget {
    return PerfChartWidget.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PerfChartWidget>): PerfChartWidget {
    const message = createBasePerfChartWidget() as any;
    message.dataSpecId = object.dataSpecId ?? "";
    return message;
  },
};

/** Service for Android performance-related data. */
export interface PerfService {
  /** Streams the data to populate the widgets defined within a DashboardState. */
  StreamDashboardData(request: StreamDashboardDataRequest): Observable<StreamDashboardDataResponse>;
}

export const PerfServiceServiceName = "google.android.perf.v1.PerfService";
export class PerfServiceClientImpl implements PerfService {
  static readonly DEFAULT_SERVICE = PerfServiceServiceName;
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || PerfServiceServiceName;
    this.rpc = rpc;
    this.StreamDashboardData = this.StreamDashboardData.bind(this);
  }
  StreamDashboardData(request: StreamDashboardDataRequest): Observable<StreamDashboardDataResponse> {
    const data = StreamDashboardDataRequest.toJSON(request);
    const result = this.rpc.serverStreamingRequest(this.service, "StreamDashboardData", data);
    return result.pipe(map((data) => StreamDashboardDataResponse.fromJSON(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: unknown): Promise<unknown>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

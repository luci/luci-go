// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v6.32.0
// source: go.chromium.org/infra/fleetconsole/api/fleetconsolerpc/admintask.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface GetDeviceACLsRequest {
  readonly deviceId: string;
  /**
   * The realm of the device. If provided, the backend will skip looking up the
   * device to find its realm.
   */
  readonly realm: string;
}

export interface GetDeviceACLsResponse {
  readonly realm: string;
  /** All groups configured to have access to the realm */
  readonly realmGroups: readonly string[];
  /** Groups the logged-in user belongs to that grant access */
  readonly userAccessGroups: readonly string[];
}

/** @deprecated */
export interface CheckAdminTaskPermissionRequest {
}

/** @deprecated */
export interface CheckAdminTaskPermissionResponse {
  readonly hasPermission: boolean;
}

export interface CheckPermissionRequest {
  /** The MDB group to check permissions for. */
  readonly group: string;
}

export interface CheckPermissionResponse {
  readonly hasPermission: boolean;
}

function createBaseGetDeviceACLsRequest(): GetDeviceACLsRequest {
  return { deviceId: "", realm: "" };
}

export const GetDeviceACLsRequest: MessageFns<GetDeviceACLsRequest> = {
  encode(message: GetDeviceACLsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.realm !== "") {
      writer.uint32(18).string(message.realm);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDeviceACLsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDeviceACLsRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.realm = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDeviceACLsRequest {
    return {
      deviceId: isSet(object.deviceId) ? globalThis.String(object.deviceId) : "",
      realm: isSet(object.realm) ? globalThis.String(object.realm) : "",
    };
  },

  toJSON(message: GetDeviceACLsRequest): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.realm !== "") {
      obj.realm = message.realm;
    }
    return obj;
  },

  create(base?: DeepPartial<GetDeviceACLsRequest>): GetDeviceACLsRequest {
    return GetDeviceACLsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDeviceACLsRequest>): GetDeviceACLsRequest {
    const message = createBaseGetDeviceACLsRequest() as any;
    message.deviceId = object.deviceId ?? "";
    message.realm = object.realm ?? "";
    return message;
  },
};

function createBaseGetDeviceACLsResponse(): GetDeviceACLsResponse {
  return { realm: "", realmGroups: [], userAccessGroups: [] };
}

export const GetDeviceACLsResponse: MessageFns<GetDeviceACLsResponse> = {
  encode(message: GetDeviceACLsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.realm !== "") {
      writer.uint32(10).string(message.realm);
    }
    for (const v of message.realmGroups) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.userAccessGroups) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDeviceACLsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDeviceACLsResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.realm = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.realmGroups.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.userAccessGroups.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDeviceACLsResponse {
    return {
      realm: isSet(object.realm) ? globalThis.String(object.realm) : "",
      realmGroups: globalThis.Array.isArray(object?.realmGroups)
        ? object.realmGroups.map((e: any) => globalThis.String(e))
        : [],
      userAccessGroups: globalThis.Array.isArray(object?.userAccessGroups)
        ? object.userAccessGroups.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GetDeviceACLsResponse): unknown {
    const obj: any = {};
    if (message.realm !== "") {
      obj.realm = message.realm;
    }
    if (message.realmGroups?.length) {
      obj.realmGroups = message.realmGroups;
    }
    if (message.userAccessGroups?.length) {
      obj.userAccessGroups = message.userAccessGroups;
    }
    return obj;
  },

  create(base?: DeepPartial<GetDeviceACLsResponse>): GetDeviceACLsResponse {
    return GetDeviceACLsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDeviceACLsResponse>): GetDeviceACLsResponse {
    const message = createBaseGetDeviceACLsResponse() as any;
    message.realm = object.realm ?? "";
    message.realmGroups = object.realmGroups?.map((e) => e) || [];
    message.userAccessGroups = object.userAccessGroups?.map((e) => e) || [];
    return message;
  },
};

function createBaseCheckAdminTaskPermissionRequest(): CheckAdminTaskPermissionRequest {
  return {};
}

export const CheckAdminTaskPermissionRequest: MessageFns<CheckAdminTaskPermissionRequest> = {
  encode(_: CheckAdminTaskPermissionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckAdminTaskPermissionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckAdminTaskPermissionRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CheckAdminTaskPermissionRequest {
    return {};
  },

  toJSON(_: CheckAdminTaskPermissionRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CheckAdminTaskPermissionRequest>): CheckAdminTaskPermissionRequest {
    return CheckAdminTaskPermissionRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CheckAdminTaskPermissionRequest>): CheckAdminTaskPermissionRequest {
    const message = createBaseCheckAdminTaskPermissionRequest() as any;
    return message;
  },
};

function createBaseCheckAdminTaskPermissionResponse(): CheckAdminTaskPermissionResponse {
  return { hasPermission: false };
}

export const CheckAdminTaskPermissionResponse: MessageFns<CheckAdminTaskPermissionResponse> = {
  encode(message: CheckAdminTaskPermissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hasPermission !== false) {
      writer.uint32(8).bool(message.hasPermission);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckAdminTaskPermissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckAdminTaskPermissionResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hasPermission = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckAdminTaskPermissionResponse {
    return { hasPermission: isSet(object.hasPermission) ? globalThis.Boolean(object.hasPermission) : false };
  },

  toJSON(message: CheckAdminTaskPermissionResponse): unknown {
    const obj: any = {};
    if (message.hasPermission !== false) {
      obj.hasPermission = message.hasPermission;
    }
    return obj;
  },

  create(base?: DeepPartial<CheckAdminTaskPermissionResponse>): CheckAdminTaskPermissionResponse {
    return CheckAdminTaskPermissionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CheckAdminTaskPermissionResponse>): CheckAdminTaskPermissionResponse {
    const message = createBaseCheckAdminTaskPermissionResponse() as any;
    message.hasPermission = object.hasPermission ?? false;
    return message;
  },
};

function createBaseCheckPermissionRequest(): CheckPermissionRequest {
  return { group: "" };
}

export const CheckPermissionRequest: MessageFns<CheckPermissionRequest> = {
  encode(message: CheckPermissionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.group !== "") {
      writer.uint32(10).string(message.group);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckPermissionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckPermissionRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.group = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckPermissionRequest {
    return { group: isSet(object.group) ? globalThis.String(object.group) : "" };
  },

  toJSON(message: CheckPermissionRequest): unknown {
    const obj: any = {};
    if (message.group !== "") {
      obj.group = message.group;
    }
    return obj;
  },

  create(base?: DeepPartial<CheckPermissionRequest>): CheckPermissionRequest {
    return CheckPermissionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CheckPermissionRequest>): CheckPermissionRequest {
    const message = createBaseCheckPermissionRequest() as any;
    message.group = object.group ?? "";
    return message;
  },
};

function createBaseCheckPermissionResponse(): CheckPermissionResponse {
  return { hasPermission: false };
}

export const CheckPermissionResponse: MessageFns<CheckPermissionResponse> = {
  encode(message: CheckPermissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hasPermission !== false) {
      writer.uint32(8).bool(message.hasPermission);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckPermissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckPermissionResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hasPermission = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckPermissionResponse {
    return { hasPermission: isSet(object.hasPermission) ? globalThis.Boolean(object.hasPermission) : false };
  },

  toJSON(message: CheckPermissionResponse): unknown {
    const obj: any = {};
    if (message.hasPermission !== false) {
      obj.hasPermission = message.hasPermission;
    }
    return obj;
  },

  create(base?: DeepPartial<CheckPermissionResponse>): CheckPermissionResponse {
    return CheckPermissionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CheckPermissionResponse>): CheckPermissionResponse {
    const message = createBaseCheckPermissionResponse() as any;
    message.hasPermission = object.hasPermission ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

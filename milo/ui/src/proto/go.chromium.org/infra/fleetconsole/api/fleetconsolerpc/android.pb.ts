// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.1
//   protoc               v3.21.12
// source: go.chromium.org/infra/fleetconsole/api/fleetconsolerpc/android.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../../../google/protobuf/timestamp.pb";
import { MonitoredRecord } from "../../internal/infra/ext/omnilab/omnilab-pubsub.pb";
import { DeviceSpec } from "./common_types.pb";

export interface AndroidDevice {
  readonly id: string;
  readonly omnilabSpec: DeviceSpec | undefined;
  readonly runTarget: string;
  readonly realm: string;
  readonly fcOfflineSince: string | undefined;
}

export interface ListAndroidDevicesRequest {
  readonly pageSize: number;
  readonly pageToken: string;
  readonly orderBy: string;
  readonly filter: string;
}

export interface ListAndroidDevicesResponse {
  readonly devices: readonly AndroidDevice[];
  readonly nextPageToken: string;
  readonly totalSize: number;
}

export interface AndroidCount {
  readonly totalDevices: number;
  readonly idleDevices: number;
  readonly busyDevices: number;
  readonly dyingDevices: number;
  readonly preppingDevices: number;
  readonly missingDevices: number;
  readonly initDevices: number;
  readonly lameduckDevices: number;
  readonly failedDevices: number;
  readonly dirtyDevices: number;
  readonly totalHosts: number;
  readonly labMissingHosts: number;
  readonly labRunningHosts: number;
}

export interface RepopulateAndroidCacheRequest {
}

export interface RepopulateAndroidCacheResponse {
}

export interface UpdateAndroidMetricsRequest {
}

export interface UpdateAndroidMetricsResponse {
}

export interface UpdateAndroidDevicesRequest {
  readonly host: MonitoredRecord | undefined;
}

export interface UpdateAndroidDevicesResponse {
}

export interface CleanupAndroidDevicesRequest {
}

export interface CleanupAndroidDevicesResponse {
}

function createBaseAndroidDevice(): AndroidDevice {
  return { id: "", omnilabSpec: undefined, runTarget: "", realm: "", fcOfflineSince: undefined };
}

export const AndroidDevice: MessageFns<AndroidDevice> = {
  encode(message: AndroidDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.omnilabSpec !== undefined) {
      DeviceSpec.encode(message.omnilabSpec, writer.uint32(18).fork()).join();
    }
    if (message.runTarget !== "") {
      writer.uint32(26).string(message.runTarget);
    }
    if (message.realm !== "") {
      writer.uint32(34).string(message.realm);
    }
    if (message.fcOfflineSince !== undefined) {
      Timestamp.encode(toTimestamp(message.fcOfflineSince), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AndroidDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAndroidDevice() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.omnilabSpec = DeviceSpec.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.runTarget = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.realm = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fcOfflineSince = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AndroidDevice {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      omnilabSpec: isSet(object.omnilabSpec) ? DeviceSpec.fromJSON(object.omnilabSpec) : undefined,
      runTarget: isSet(object.runTarget) ? globalThis.String(object.runTarget) : "",
      realm: isSet(object.realm) ? globalThis.String(object.realm) : "",
      fcOfflineSince: isSet(object.fcOfflineSince) ? globalThis.String(object.fcOfflineSince) : undefined,
    };
  },

  toJSON(message: AndroidDevice): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.omnilabSpec !== undefined) {
      obj.omnilabSpec = DeviceSpec.toJSON(message.omnilabSpec);
    }
    if (message.runTarget !== "") {
      obj.runTarget = message.runTarget;
    }
    if (message.realm !== "") {
      obj.realm = message.realm;
    }
    if (message.fcOfflineSince !== undefined) {
      obj.fcOfflineSince = message.fcOfflineSince;
    }
    return obj;
  },

  create(base?: DeepPartial<AndroidDevice>): AndroidDevice {
    return AndroidDevice.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AndroidDevice>): AndroidDevice {
    const message = createBaseAndroidDevice() as any;
    message.id = object.id ?? "";
    message.omnilabSpec = (object.omnilabSpec !== undefined && object.omnilabSpec !== null)
      ? DeviceSpec.fromPartial(object.omnilabSpec)
      : undefined;
    message.runTarget = object.runTarget ?? "";
    message.realm = object.realm ?? "";
    message.fcOfflineSince = object.fcOfflineSince ?? undefined;
    return message;
  },
};

function createBaseListAndroidDevicesRequest(): ListAndroidDevicesRequest {
  return { pageSize: 0, pageToken: "", orderBy: "", filter: "" };
}

export const ListAndroidDevicesRequest: MessageFns<ListAndroidDevicesRequest> = {
  encode(message: ListAndroidDevicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pageSize !== 0) {
      writer.uint32(8).int32(message.pageSize);
    }
    if (message.pageToken !== "") {
      writer.uint32(18).string(message.pageToken);
    }
    if (message.orderBy !== "") {
      writer.uint32(26).string(message.orderBy);
    }
    if (message.filter !== "") {
      writer.uint32(34).string(message.filter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAndroidDevicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAndroidDevicesRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.orderBy = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.filter = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAndroidDevicesRequest {
    return {
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageToken: isSet(object.pageToken) ? globalThis.String(object.pageToken) : "",
      orderBy: isSet(object.orderBy) ? globalThis.String(object.orderBy) : "",
      filter: isSet(object.filter) ? globalThis.String(object.filter) : "",
    };
  },

  toJSON(message: ListAndroidDevicesRequest): unknown {
    const obj: any = {};
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== "") {
      obj.pageToken = message.pageToken;
    }
    if (message.orderBy !== "") {
      obj.orderBy = message.orderBy;
    }
    if (message.filter !== "") {
      obj.filter = message.filter;
    }
    return obj;
  },

  create(base?: DeepPartial<ListAndroidDevicesRequest>): ListAndroidDevicesRequest {
    return ListAndroidDevicesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListAndroidDevicesRequest>): ListAndroidDevicesRequest {
    const message = createBaseListAndroidDevicesRequest() as any;
    message.pageSize = object.pageSize ?? 0;
    message.pageToken = object.pageToken ?? "";
    message.orderBy = object.orderBy ?? "";
    message.filter = object.filter ?? "";
    return message;
  },
};

function createBaseListAndroidDevicesResponse(): ListAndroidDevicesResponse {
  return { devices: [], nextPageToken: "", totalSize: 0 };
}

export const ListAndroidDevicesResponse: MessageFns<ListAndroidDevicesResponse> = {
  encode(message: ListAndroidDevicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.devices) {
      AndroidDevice.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextPageToken !== "") {
      writer.uint32(18).string(message.nextPageToken);
    }
    if (message.totalSize !== 0) {
      writer.uint32(24).int32(message.totalSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAndroidDevicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAndroidDevicesResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.devices.push(AndroidDevice.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextPageToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalSize = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAndroidDevicesResponse {
    return {
      devices: globalThis.Array.isArray(object?.devices)
        ? object.devices.map((e: any) => AndroidDevice.fromJSON(e))
        : [],
      nextPageToken: isSet(object.nextPageToken) ? globalThis.String(object.nextPageToken) : "",
      totalSize: isSet(object.totalSize) ? globalThis.Number(object.totalSize) : 0,
    };
  },

  toJSON(message: ListAndroidDevicesResponse): unknown {
    const obj: any = {};
    if (message.devices?.length) {
      obj.devices = message.devices.map((e) => AndroidDevice.toJSON(e));
    }
    if (message.nextPageToken !== "") {
      obj.nextPageToken = message.nextPageToken;
    }
    if (message.totalSize !== 0) {
      obj.totalSize = Math.round(message.totalSize);
    }
    return obj;
  },

  create(base?: DeepPartial<ListAndroidDevicesResponse>): ListAndroidDevicesResponse {
    return ListAndroidDevicesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListAndroidDevicesResponse>): ListAndroidDevicesResponse {
    const message = createBaseListAndroidDevicesResponse() as any;
    message.devices = object.devices?.map((e) => AndroidDevice.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? "";
    message.totalSize = object.totalSize ?? 0;
    return message;
  },
};

function createBaseAndroidCount(): AndroidCount {
  return {
    totalDevices: 0,
    idleDevices: 0,
    busyDevices: 0,
    dyingDevices: 0,
    preppingDevices: 0,
    missingDevices: 0,
    initDevices: 0,
    lameduckDevices: 0,
    failedDevices: 0,
    dirtyDevices: 0,
    totalHosts: 0,
    labMissingHosts: 0,
    labRunningHosts: 0,
  };
}

export const AndroidCount: MessageFns<AndroidCount> = {
  encode(message: AndroidCount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalDevices !== 0) {
      writer.uint32(8).int32(message.totalDevices);
    }
    if (message.idleDevices !== 0) {
      writer.uint32(16).int32(message.idleDevices);
    }
    if (message.busyDevices !== 0) {
      writer.uint32(24).int32(message.busyDevices);
    }
    if (message.dyingDevices !== 0) {
      writer.uint32(32).int32(message.dyingDevices);
    }
    if (message.preppingDevices !== 0) {
      writer.uint32(40).int32(message.preppingDevices);
    }
    if (message.missingDevices !== 0) {
      writer.uint32(48).int32(message.missingDevices);
    }
    if (message.initDevices !== 0) {
      writer.uint32(56).int32(message.initDevices);
    }
    if (message.lameduckDevices !== 0) {
      writer.uint32(64).int32(message.lameduckDevices);
    }
    if (message.failedDevices !== 0) {
      writer.uint32(72).int32(message.failedDevices);
    }
    if (message.dirtyDevices !== 0) {
      writer.uint32(80).int32(message.dirtyDevices);
    }
    if (message.totalHosts !== 0) {
      writer.uint32(88).int32(message.totalHosts);
    }
    if (message.labMissingHosts !== 0) {
      writer.uint32(96).int32(message.labMissingHosts);
    }
    if (message.labRunningHosts !== 0) {
      writer.uint32(104).int32(message.labRunningHosts);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AndroidCount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAndroidCount() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalDevices = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.idleDevices = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.busyDevices = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dyingDevices = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.preppingDevices = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.missingDevices = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.initDevices = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.lameduckDevices = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.failedDevices = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.dirtyDevices = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.totalHosts = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.labMissingHosts = reader.int32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.labRunningHosts = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AndroidCount {
    return {
      totalDevices: isSet(object.totalDevices) ? globalThis.Number(object.totalDevices) : 0,
      idleDevices: isSet(object.idleDevices) ? globalThis.Number(object.idleDevices) : 0,
      busyDevices: isSet(object.busyDevices) ? globalThis.Number(object.busyDevices) : 0,
      dyingDevices: isSet(object.dyingDevices) ? globalThis.Number(object.dyingDevices) : 0,
      preppingDevices: isSet(object.preppingDevices) ? globalThis.Number(object.preppingDevices) : 0,
      missingDevices: isSet(object.missingDevices) ? globalThis.Number(object.missingDevices) : 0,
      initDevices: isSet(object.initDevices) ? globalThis.Number(object.initDevices) : 0,
      lameduckDevices: isSet(object.lameduckDevices) ? globalThis.Number(object.lameduckDevices) : 0,
      failedDevices: isSet(object.failedDevices) ? globalThis.Number(object.failedDevices) : 0,
      dirtyDevices: isSet(object.dirtyDevices) ? globalThis.Number(object.dirtyDevices) : 0,
      totalHosts: isSet(object.totalHosts) ? globalThis.Number(object.totalHosts) : 0,
      labMissingHosts: isSet(object.labMissingHosts) ? globalThis.Number(object.labMissingHosts) : 0,
      labRunningHosts: isSet(object.labRunningHosts) ? globalThis.Number(object.labRunningHosts) : 0,
    };
  },

  toJSON(message: AndroidCount): unknown {
    const obj: any = {};
    if (message.totalDevices !== 0) {
      obj.totalDevices = Math.round(message.totalDevices);
    }
    if (message.idleDevices !== 0) {
      obj.idleDevices = Math.round(message.idleDevices);
    }
    if (message.busyDevices !== 0) {
      obj.busyDevices = Math.round(message.busyDevices);
    }
    if (message.dyingDevices !== 0) {
      obj.dyingDevices = Math.round(message.dyingDevices);
    }
    if (message.preppingDevices !== 0) {
      obj.preppingDevices = Math.round(message.preppingDevices);
    }
    if (message.missingDevices !== 0) {
      obj.missingDevices = Math.round(message.missingDevices);
    }
    if (message.initDevices !== 0) {
      obj.initDevices = Math.round(message.initDevices);
    }
    if (message.lameduckDevices !== 0) {
      obj.lameduckDevices = Math.round(message.lameduckDevices);
    }
    if (message.failedDevices !== 0) {
      obj.failedDevices = Math.round(message.failedDevices);
    }
    if (message.dirtyDevices !== 0) {
      obj.dirtyDevices = Math.round(message.dirtyDevices);
    }
    if (message.totalHosts !== 0) {
      obj.totalHosts = Math.round(message.totalHosts);
    }
    if (message.labMissingHosts !== 0) {
      obj.labMissingHosts = Math.round(message.labMissingHosts);
    }
    if (message.labRunningHosts !== 0) {
      obj.labRunningHosts = Math.round(message.labRunningHosts);
    }
    return obj;
  },

  create(base?: DeepPartial<AndroidCount>): AndroidCount {
    return AndroidCount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AndroidCount>): AndroidCount {
    const message = createBaseAndroidCount() as any;
    message.totalDevices = object.totalDevices ?? 0;
    message.idleDevices = object.idleDevices ?? 0;
    message.busyDevices = object.busyDevices ?? 0;
    message.dyingDevices = object.dyingDevices ?? 0;
    message.preppingDevices = object.preppingDevices ?? 0;
    message.missingDevices = object.missingDevices ?? 0;
    message.initDevices = object.initDevices ?? 0;
    message.lameduckDevices = object.lameduckDevices ?? 0;
    message.failedDevices = object.failedDevices ?? 0;
    message.dirtyDevices = object.dirtyDevices ?? 0;
    message.totalHosts = object.totalHosts ?? 0;
    message.labMissingHosts = object.labMissingHosts ?? 0;
    message.labRunningHosts = object.labRunningHosts ?? 0;
    return message;
  },
};

function createBaseRepopulateAndroidCacheRequest(): RepopulateAndroidCacheRequest {
  return {};
}

export const RepopulateAndroidCacheRequest: MessageFns<RepopulateAndroidCacheRequest> = {
  encode(_: RepopulateAndroidCacheRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RepopulateAndroidCacheRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRepopulateAndroidCacheRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RepopulateAndroidCacheRequest {
    return {};
  },

  toJSON(_: RepopulateAndroidCacheRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RepopulateAndroidCacheRequest>): RepopulateAndroidCacheRequest {
    return RepopulateAndroidCacheRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RepopulateAndroidCacheRequest>): RepopulateAndroidCacheRequest {
    const message = createBaseRepopulateAndroidCacheRequest() as any;
    return message;
  },
};

function createBaseRepopulateAndroidCacheResponse(): RepopulateAndroidCacheResponse {
  return {};
}

export const RepopulateAndroidCacheResponse: MessageFns<RepopulateAndroidCacheResponse> = {
  encode(_: RepopulateAndroidCacheResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RepopulateAndroidCacheResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRepopulateAndroidCacheResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RepopulateAndroidCacheResponse {
    return {};
  },

  toJSON(_: RepopulateAndroidCacheResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RepopulateAndroidCacheResponse>): RepopulateAndroidCacheResponse {
    return RepopulateAndroidCacheResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RepopulateAndroidCacheResponse>): RepopulateAndroidCacheResponse {
    const message = createBaseRepopulateAndroidCacheResponse() as any;
    return message;
  },
};

function createBaseUpdateAndroidMetricsRequest(): UpdateAndroidMetricsRequest {
  return {};
}

export const UpdateAndroidMetricsRequest: MessageFns<UpdateAndroidMetricsRequest> = {
  encode(_: UpdateAndroidMetricsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAndroidMetricsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAndroidMetricsRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): UpdateAndroidMetricsRequest {
    return {};
  },

  toJSON(_: UpdateAndroidMetricsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<UpdateAndroidMetricsRequest>): UpdateAndroidMetricsRequest {
    return UpdateAndroidMetricsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<UpdateAndroidMetricsRequest>): UpdateAndroidMetricsRequest {
    const message = createBaseUpdateAndroidMetricsRequest() as any;
    return message;
  },
};

function createBaseUpdateAndroidMetricsResponse(): UpdateAndroidMetricsResponse {
  return {};
}

export const UpdateAndroidMetricsResponse: MessageFns<UpdateAndroidMetricsResponse> = {
  encode(_: UpdateAndroidMetricsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAndroidMetricsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAndroidMetricsResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): UpdateAndroidMetricsResponse {
    return {};
  },

  toJSON(_: UpdateAndroidMetricsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<UpdateAndroidMetricsResponse>): UpdateAndroidMetricsResponse {
    return UpdateAndroidMetricsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<UpdateAndroidMetricsResponse>): UpdateAndroidMetricsResponse {
    const message = createBaseUpdateAndroidMetricsResponse() as any;
    return message;
  },
};

function createBaseUpdateAndroidDevicesRequest(): UpdateAndroidDevicesRequest {
  return { host: undefined };
}

export const UpdateAndroidDevicesRequest: MessageFns<UpdateAndroidDevicesRequest> = {
  encode(message: UpdateAndroidDevicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== undefined) {
      MonitoredRecord.encode(message.host, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAndroidDevicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAndroidDevicesRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = MonitoredRecord.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateAndroidDevicesRequest {
    return { host: isSet(object.host) ? MonitoredRecord.fromJSON(object.host) : undefined };
  },

  toJSON(message: UpdateAndroidDevicesRequest): unknown {
    const obj: any = {};
    if (message.host !== undefined) {
      obj.host = MonitoredRecord.toJSON(message.host);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateAndroidDevicesRequest>): UpdateAndroidDevicesRequest {
    return UpdateAndroidDevicesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateAndroidDevicesRequest>): UpdateAndroidDevicesRequest {
    const message = createBaseUpdateAndroidDevicesRequest() as any;
    message.host = (object.host !== undefined && object.host !== null)
      ? MonitoredRecord.fromPartial(object.host)
      : undefined;
    return message;
  },
};

function createBaseUpdateAndroidDevicesResponse(): UpdateAndroidDevicesResponse {
  return {};
}

export const UpdateAndroidDevicesResponse: MessageFns<UpdateAndroidDevicesResponse> = {
  encode(_: UpdateAndroidDevicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateAndroidDevicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateAndroidDevicesResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): UpdateAndroidDevicesResponse {
    return {};
  },

  toJSON(_: UpdateAndroidDevicesResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<UpdateAndroidDevicesResponse>): UpdateAndroidDevicesResponse {
    return UpdateAndroidDevicesResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<UpdateAndroidDevicesResponse>): UpdateAndroidDevicesResponse {
    const message = createBaseUpdateAndroidDevicesResponse() as any;
    return message;
  },
};

function createBaseCleanupAndroidDevicesRequest(): CleanupAndroidDevicesRequest {
  return {};
}

export const CleanupAndroidDevicesRequest: MessageFns<CleanupAndroidDevicesRequest> = {
  encode(_: CleanupAndroidDevicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CleanupAndroidDevicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCleanupAndroidDevicesRequest() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CleanupAndroidDevicesRequest {
    return {};
  },

  toJSON(_: CleanupAndroidDevicesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CleanupAndroidDevicesRequest>): CleanupAndroidDevicesRequest {
    return CleanupAndroidDevicesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CleanupAndroidDevicesRequest>): CleanupAndroidDevicesRequest {
    const message = createBaseCleanupAndroidDevicesRequest() as any;
    return message;
  },
};

function createBaseCleanupAndroidDevicesResponse(): CleanupAndroidDevicesResponse {
  return {};
}

export const CleanupAndroidDevicesResponse: MessageFns<CleanupAndroidDevicesResponse> = {
  encode(_: CleanupAndroidDevicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CleanupAndroidDevicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCleanupAndroidDevicesResponse() as any;
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CleanupAndroidDevicesResponse {
    return {};
  },

  toJSON(_: CleanupAndroidDevicesResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CleanupAndroidDevicesResponse>): CleanupAndroidDevicesResponse {
    return CleanupAndroidDevicesResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CleanupAndroidDevicesResponse>): CleanupAndroidDevicesResponse {
    const message = createBaseCleanupAndroidDevicesResponse() as any;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // The following settings are needed because Vite relies on ES modules.
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true
  }
}
